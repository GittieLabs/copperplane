"""SPEC-210 / CTX-210.1: the consideration record and its trigger discipline.

Stub created with the context so the Testing Requirements Matrix names a real
file, per `/new-context`'s own rule. Phase 2 fills it.
"""
import unittest


class ConsiderationsPlaceholder(unittest.TestCase):
    def test_the_module_is_not_built_yet(self):
        self.skipTest("CTX-210.1 Phase 2 has not run yet.")


if __name__ == "__main__":
    unittest.main()
