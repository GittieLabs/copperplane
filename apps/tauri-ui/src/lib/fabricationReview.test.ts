import { describe, expect, it } from 'vitest'

/** SPEC-340: the client for SPEC-114's three `fabrication.*` routes.
 *  Planned by CTX-340.1 Phase 2. */
describe('fabricationReview client', () => {
  it.todo('CTX-340.1 Phase 2 has not been implemented yet')
  it('placeholder so the suite has a real file to run', () => {
    expect(true).toBe(true)
  })
})
