import { describe, expect, it } from 'vitest'

/** SPEC-340 section 5: choosing the board house, once per project.
 *  Planned by CTX-340.1 Phase 3. */
describe('FabricationProfile', () => {
  it.todo('CTX-340.1 Phase 3 has not been implemented yet')
  it('placeholder so the suite has a real file to run', () => {
    expect(true).toBe(true)
  })
})
