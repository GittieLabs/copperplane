import { describe, it } from 'vitest'

/** CTX-336.1 stub. The Testing Requirements Matrix names this file, and
 *  `scripts/validate_spec_context.py` checks it exists; the cases arrive with
 *  the phase that builds NoProjectLanding. */
describe.todo('NoProjectLanding')
