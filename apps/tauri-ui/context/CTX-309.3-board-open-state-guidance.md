---
id: CTX-309.3
spec_ref: "../specs/SPEC-309-board-advisor.md"
title: "Implementation Context: Board Open-State Guidance & Multi-Board Selection"
status: Planned
author: "AI Agent"
branch: "feat/CTX-309.3-board-open-state-guidance"
created: 2026-08-18
last_touched: 2026-08-18
version_included: "unreleased"
commit_hashes:
  - "b3537cf - board open-state guidance and multi-board selection"
  - "PENDING - list-first board picker, Open KiCad action (revision below)"
---

# CTX-309.3: Implementation Context: Board Open-State Guidance & Multi-Board Selection

> **Spec Reference:** Links to [SPEC-309](../specs/SPEC-309-board-advisor.md)

Real user feedback exercising the actual running app: "Check Board" surfaced a raw exception
message (a stale scratch path from an unrelated earlier session) with no guidance on what to do
about it. `CTX-309.1`'s own backend silently narrows `get_open_documents(DOCTYPE_PCB)`'s real list
down to `docs[0]` -- discarding any board past the first -- and `CTX-309.2`'s own frontend renders
every failure as flat red text, never distinguishing "nothing open" (a normal, expected state
`kicad_check_board`'s own docstring already says should be "report[ed] plainly," but never actually
was) from a genuine connection failure. This closes that gap.

**This context was revised in place before its PR merged** (see §4 Deviation 2) once the first cut
was exercised live in the actual running app: it still silently auto-resolved a single open board
without ever showing the user which one, and offered no way to actually open KiCad when none was
running. The description below is the final, shipped design; §4 records both the original plan and
why it changed.

**Scope grew again (see §4 Deviation 5) from UI polish into a real architectural split.** Real,
continued use of the running app raised a legitimate product question this context didn't originally
plan for: why does Schematic (ERC) checking live under the "PCB" tab at all? Reading `SPEC-300`
(the product IA spec) directly answered it -- ERC belongs to a separate "Schematic Advisor" stage,
not "PCB Advisor" -- and the user confirmed moving it. This also produced a genuinely new backend
capability (deriving a schematic's path from an open board's own project, since KiCad's IPC still
can't list open schematics at all) that wasn't part of this context's original plan either.

## 1. Feature Definition & Execution Plan

### Phase 1: real, live investigation before writing any code
*   [x] Confirmed real, against this machine's own actually-running KiCad instance (the same live
    IPC socket `test_004_real_round_trip_against_a_live_kicad_instance` already uses): with no PCB
    Editor window open, `get_open_documents(DOCTYPE_PCB)` doesn't return an empty list -- it raises a
    real `ApiError`: `"KiCad returned error: no handler available for request of type
    kiapi.common.commands.GetOpenDocuments"`. The handler isn't registered until a PCB Editor window
    is actually open, the same real constraint `CTX-108.1`'s own Deviation 2 already documented for
    the write path.
*   [x] Confirmed real, by reading `kicad_bridge.get_open_board_path()`'s existing exception
    handling: it catches `ApiError` in the same branch as a genuine `KiCadConnectionError`, calls
    `reset_connection()`, and raises `KiCadUnavailableError("Lost connection to KiCad mid-request.
    It may have been closed.")` -- a real, live-confirmed **bug**: the actual, current, everyday
    "no board open" state was being reported to the user as if KiCad had just disconnected, which is
    not what happened at all.
*   [x] Confirmed real, by reading `kicad_bridge.get_open_board_path()`'s successful path: `docs[0]`
    is returned unconditionally when `docs` is non-empty -- any board past the first, if more than
    one PCB Editor window happens to be open, is silently discarded with no way for the caller (or
    the user) to know it existed.
*   [x] Confirmed real, by reading `BoardAdvisor.tsx`: `boardError` is rendered as flat
    `text-red-400` text from `err.message` with zero branching on what kind of failure occurred --
    every failure mode looks identical to the user today.
*   [x] Confirmed real, by the user directly exercising the running app after the first cut of this
    context landed on this branch (pre-merge): a single open board was still auto-checked with no
    indication of *which* board, and the `no_board_open` state told the user to open KiCad's
    Preferences → Plugins menu without ever mentioning the Tauri app itself has no such menu, or
    offering to launch KiCad at all. Live process inspection (`ps aux`, `osascript` process listing)
    on this same dev machine confirmed KiCad genuinely was not running in both instances this came
    up -- the guidance gap was real, not hypothetical.

### Phase 2: `kicad_bridge.py` -- a real list, not a silent pick
*   [x] Replace `get_open_board_path() -> str | None` with `list_open_boards() -> list[str]`: returns
    every real open board's resolved path, never narrowed to one. Catches the real `"no handler
    available"` `ApiError` specifically and returns `[]` for it (a normal "nothing open" state, not
    a `reset_connection()`-worthy failure) -- every other `ApiError`/`KiCadConnectionError` still
    resets the connection and raises `KiCadUnavailableError` exactly as before, since those really
    are connection failures.

### Phase 3: `daemon.py` -- listing and checking are two separate real routes
*   [x] `kicad_list_open_boards()`: a new, fast, synchronous route (`kicad.list_open_boards`, not in
    `ASYNC_ROUTES`) that calls `list_open_boards()` and returns one of two real, structured shapes:
    *   Zero open: `{"status": "no_board_open"}`.
    *   One or more open: `{"status": "boards_found", "candidates": [{"path", "label"}, ...]}`,
        `label` a real, human-legible `os.path.basename(path)` -- **always** a list, even for a
        single board, so the frontend always shows the user which board it's about to check rather
        than silently picking for them.
*   [x] `kicad_check_board(pcb_path)`: `pcb_path` is now a **required** parameter -- all
    resolution/auto-pick logic moves to `kicad_list_open_boards()` above. Given an explicit path, it
    always runs DRC directly and returns the violations result tagged `"status": "ok"`.

### Phase 4: launching KiCad directly from the app
*   [x] `core/tauri-rust/src/lib.rs`: new `open_kicad` Tauri command. macOS uses `open -a KiCad`
    (Launch Services app-name lookup, verified live on this dev machine -- KiCad's own process
    appeared within ~2s of running it); Windows/Linux fall back to invoking `kicad` directly on
    PATH, the same assumption `find_kicad_cli` already makes elsewhere in this app --
    **not verified on those platforms**, named honestly in the command's own doc comment.

### Phase 5: frontend -- always show the real list, offer to launch KiCad
*   [x] `lib/boardAdvisor.ts`: `listOpenBoards()` wraps `kicad.list_open_boards` (plain `dispatch`,
    matching `kicad.get_version`'s own sync-route precedent); `openKicad()` wraps the new
    `open_kicad` Tauri command; `checkBoard(pcbPath)` now always takes an explicit path and returns a
    plain `CheckResult` or throws -- no more discriminated-union return type, since resolution no
    longer happens inside it.
*   [x] `BoardAdvisor.tsx`: the Board (DRC) section now scans for open boards as soon as the screen
    mounts (`useEffect`, no click required) and always renders one of:
    *   `no_board_open`: a real, concrete numbered walkthrough (open KiCad, open the project, open
        its PCB Editor window specifically, confirm the IPC API is enabled in Preferences →
        Plugins) plus a real **"Open KiCad"** button (shows "Opening…" while pending) and a
        **"Refresh"** button to re-scan once the user has done so.
    *   `boards_found`: a real, clickable list of every open board's `label` and full `path` --
        singular copy ("Board open in KiCad:") for exactly one, plural ("Boards open in KiCad — pick
        one to check:") for more than one -- each click invoking `checkBoard(candidate.path)` with
        that explicit path, plus its own "Refresh" button.
    *   A genuine connection failure scanning for open boards renders as flat error text with a
        "Retry" action -- a real, different failure mode from "nothing open," not blurred together
        with it.
    *   The check result itself (violations list, clean-board message, or a check-time error) renders
        below the picker, independent of it -- picking a different board re-runs the check without
        losing the list.

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | Real, live confirmation against this machine's own running KiCad instance that `get_open_documents(DOCTYPE_PCB)` raises a real ApiError containing "no handler available" when no PCB Editor window is open, and that the pre-existing code mishandled it as a connection failure | N/A | Passed |
| `TEST-002` | `list_open_boards()` returns every real open board's path, not just the first; the real "no handler available" ApiError returns an empty list rather than raising (and does NOT reset the connection); a genuine connection error still resets and raises `KiCadUnavailableError`; a different real ApiError also still resets and raises | `services/python-daemon/tests/test_kicad_bridge.py` | Passed |
| `TEST-003` | `daemon.kicad_list_open_boards()` returns `no_board_open` for zero open boards and `boards_found` with real candidate labels (derived via `os.path.basename`) for one or more; the route is registered as synchronous, not async, and omitted when `kicad_bridge` failed to import; `daemon.kicad_check_board(pcb_path)` requires an explicit path and always runs DRC directly, tagged `status: ok` | `services/python-daemon/tests/test_daemon.py` | Passed |
| `TEST-004` | Real, live round trip against this machine's actual running KiCad instance -- ran for real (a live KiCad 10.0.3 process with an active IPC socket was present on this dev machine during implementation), not skipped | `services/python-daemon/tests/test_kicad_bridge.py` | Passed |
| `TEST-005` | Frontend: `BoardAdvisor.tsx` scans for open boards on mount with no click required; a single open board is still shown as a real, explicit, clickable item (never auto-checked); more than one open board shows every real candidate; both `no_board_open` and a genuine connection failure render through the same calm, non-red guidance card with working "Open KiCad" and "Refresh" actions; a failed "Open KiCad" shows its real error via its own dedicated state, kept visible rather than folded into the calm copy; "Refresh" re-scans and picks up a board opened since the last scan; "Switch to KiCad" is offered alongside the candidate list; the clicked candidate stays visibly selected (`aria-pressed`) and its path is not repeated a second time under the result; checking shows real feedback naming the board being checked; a truncated violations count is surfaced, not silently dropped | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | Passed |
| `TEST-006` | `core/tauri-rust`: `open_kicad` compiles and is registered in `generate_handler!`; no automated test for the real process spawn itself (real OS-level process spawning is unsafe/environment-dependent to assert on in CI) | `core/tauri-rust/src/lib.rs` | Passed (compile-only) |
| `TEST-007` | `App.tsx` keeps `BoardAdvisor` mounted (hidden via CSS) across every area tab instead of unmounting it on `pcb` deselect: a finished check survives switching to another area and back, with no re-scan; switching to a genuinely different project resets the previous project's selected board and result | `apps/tauri-ui/src/App.test.tsx` | Passed |
| `TEST-008` | `BoardAdvisor` re-rendered with the same `projectName` keeps its board-list call count at one and its result visible (models App.tsx's own re-render on a tab switch); re-rendered with a *different* `projectName` clears the previous project's selected board and result | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | Passed |
| `TEST-009` | `kicad_bridge.list_project_schematics()` derives a schematic path from an open board's own real `project.name`/`project.path` (not its possibly-differently-named `board_filename`); a derived path that doesn't exist on disk is never returned; two open boards from the same project dedupe to one schematic; a genuine connection failure still resets and raises; the "no handler available" state returns an empty list without resetting the connection | `services/python-daemon/tests/test_kicad_bridge.py` | Passed |
| `TEST-010` | Real, live round trip against this machine's actual running KiCad instance with a real project open -- derived the real `NFC_Reader_ESP32.kicad_sch` path from the real open `NFC_Reader_ESP32.kicad_pcb`, confirmed to exist on disk; ran for real, not skipped | `services/python-daemon/tests/test_kicad_bridge.py` | Passed |
| `TEST-011` | `daemon.kicad_list_project_schematics()` mirrors `kicad_list_open_boards`'s own two-state shape (`no_schematic_found` / `schematics_found` with real candidate labels); registered as a sync route, omitted when `kicad_bridge` failed to import | `services/python-daemon/tests/test_daemon.py` | Passed |
| `TEST-012` | Frontend: `SchematicAdvisor.tsx` (new, extracted from `BoardAdvisor.tsx`) mirrors the Board (DRC) list-first UX -- scans on mount, shows a real clickable list of derived schematics (even a single one), the same calm merged guidance card for `no_schematic_found`/connection failure, "Switch to KiCad"/"Refresh" on the found-candidates state, a real "Pick file manually…" fallback that still runs a full check (covering a schematic not tied to any open board), selection highlighting, descriptive checking copy, and the same tab-switch/project-switch persistence contract as `BoardAdvisor` | `apps/tauri-ui/src/components/SchematicAdvisor.test.tsx` | Passed |
| `TEST-013` | `App.tsx`'s Schematic tab renders the real `SchematicAdvisor`, not a `NotBuiltPlaceholder`; a finished check survives switching to another area and back; switching to a genuinely different project resets it -- mirrors `TEST-007` for the PCB tab | `apps/tauri-ui/src/App.test.tsx` | Passed |
| `TEST-014` | `BoardAdvisor.tsx`/`ViolationsList` (now extracted into its own shared `ViolationsList.tsx`, used by both `BoardAdvisor` and `SchematicAdvisor`) retains every pre-existing Board (DRC) behavior after the Schematic section's removal -- no regression from the split | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | Passed |

Full suites (after all four in-place revisions): `python3 -m pytest services/python-daemon/` --
302 passed, 1 skipped (pre-existing, unrelated). `npx vitest run` (apps/tauri-ui) -- 184 passed.
`npx tsc --noEmit` -- clean. `cargo check`/`cargo test` (core/tauri-rust, unchanged this round) --
clean, 32 passed.

**Not verified:** the multi-board (`boards_found`/`schematics_found` with 2+ candidates) state has
not been exercised against a real, live KiCad instance with two PCB Editor windows genuinely open at
once -- doing so requires manually opening two real boards in the KiCad desktop app, a real user
action this session can request but not perform itself. `open_kicad` is verified live only on macOS,
on this one dev machine, and only for launching KiCad from a closed state -- its behavior with KiCad
already running (does `open -a KiCad` bring an existing window forward, or launch a second instance)
has not been directly observed. The Windows/Linux `kicad`-on-PATH fallback remains unverified. The
tab-switch/project-switch persistence fixes (`TEST-007`/`TEST-008`/`TEST-013`) are verified against
jsdom-rendered mocks, not yet against the real running Tauri window for the *Schematic* tab
specifically (the PCB tab's own version of this **was** confirmed live by the user). This round did
not add cross-*restart* persistence (a `localStorage` cache surviving a full app quit) -- only
in-memory persistence across tab and area switches within one running session. The schematic-path
derivation itself (`TEST-009`/`TEST-010`) **was** confirmed live against a real project on this dev
machine (`NFC_Reader_ESP32`) -- a real win this round, not just mocked coverage. Re-testing
`get_open_documents(DOCTYPE_SCHEMATIC)` with a Schematic Editor window *specifically* open (as
opposed to just a PCB Editor window and project) was not done -- KiCad was not running with that
window open during this session's own testing; if it turns out KiCad's IPC does support schematic
listing once that specific window is open (unlike the PCB case's own transient gap), that would be a
real opportunity to simplify this derivation away in a future context, not something assumed here.

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-08-18 | Phase 1-5 (original) | Real, live investigation + bug fix, three-way response envelope, frontend guidance UI, tests | `b3537cf` |
| 2026-08-18 | Phase 1-5 (revision) | List-first picker (`kicad.list_open_boards`), required-path `kicad.check_board`, `open_kicad` Tauri command, rewritten frontend + tests | `74455b2`, `04b0733` |
| 2026-08-18 | Phase 5 (second revision) | Merged the `no_board_open`/connection-failure states into one calm, non-red guidance card; added "Switch to KiCad" + a real hint to the multi-board picker; fixed long-path overflow; added descriptive checking-state copy; highlighted (not duplicated) the picked board | `55ae3c1` |
| 2026-08-18 | Phase 5 (third revision) | `App.tsx` keeps `BoardAdvisor` mounted across area tabs (project-scoped reset instead); Schematic (ERC) explains why it's a manual file picker | `2fb46d4` |
| 2026-08-18 | Phase 5 (fourth revision) | Real schematic-path derivation (`kicad_bridge.list_project_schematics`, `daemon.kicad_list_project_schematics`); extracted `SchematicAdvisor.tsx` + shared `ViolationsList.tsx`; moved ERC checking to its own Schematic tab per `SPEC-300`'s stage-machine design; removed the meaningless-to-a-user `SPEC-309 §2` citation from the UI copy | *(pending commit)* |

## 4. Plan Drift & Architectural Changes

*   **Deviation 1: found by real, live testing on this actual dev machine's own running KiCad
    instance, not assumed from `SPEC-309`'s own prior research.** `SPEC-309` §2 documented that
    `DOCTYPE_PCB` auto-resolution "returns a real, resolvable full path... for whatever board is
    currently open," which is true only once a board *is* open -- the zero-open-boards case wasn't
    live-tested at spec time, and turned out to raise a real `ApiError` rather than return an empty
    list the way `get_open_documents` might be assumed to behave from its name alone. This context's
    own real testing caught it before shipping the new guidance UI on top of a still-mislabeled
    error.

*   **Deviation 2: the original design (three-way envelope inside `kicad_check_board`, auto-resolve
    on exactly one board) was replaced before this PR merged, based on the user's own real use of
    the running app -- not a hypothetical concern raised at design time.** The first cut kept
    `kicad_check_board(pcb_path=None)` doing double duty: resolving *and* checking, auto-running DRC
    silently whenever exactly one board happened to be open. Exercising the actual app surfaced two
    real problems that design review alone hadn't: (1) a novice had no way to tell *which* board was
    about to be checked before it ran, since the single-board case produced no picker at all, and (2)
    when zero boards were open, the guidance text described KiCad's own Preferences → Plugins menu
    without ever addressing that the user had been looking for it inside the *Tauri app*, and never
    offered to actually launch KiCad. I predicted the single-board auto-resolve would read as a
    helpful shortcut; it read as opaque instead -- a real user, not a hypothetical one, said so after
    clicking through it. The fix required a real architectural split, not a copy change: listing
    (`kicad.list_open_boards`) and checking (`kicad.check_board`) became two separate routes so the
    frontend can always show a real list before ever running a check, plus a genuinely new
    capability (`open_kicad`) neither the original spec nor the original context anticipated needing.

*   **Deviation 3: a second real click-through, after Deviation 2 had already shipped its own fix,
    surfaced six more concrete problems -- none of them design-review guesses.** The user restarted
    the actual `cargo tauri dev` app, opened the PCB tab cold (KiCad not yet running), opened KiCad
    via the new "Open KiCad" button, opened a real board in it, and reported exactly what the UI did
    at each step. That surfaced: (1) the very first visit to the tab rendered "could not connect" in
    alarming red before the user had any reason to have opened KiCad yet -- I had predicted
    Deviation 2's fix (adding "Open KiCad" to the connection-failure branch) was sufficient; it
    wasn't, because the *styling* still read as a failure even with the right action attached; (2)
    the multi-board picker had no way to open a *different* board without leaving the app entirely;
    (3) a long real path (`/Users/keithelliott/repos/PCBs/NFC_Reader_ESP32/NFC_Reader_ESP32.kicad_pcb`)
    overflowed its own box instead of wrapping; (4) clicking a candidate showed only a bare
    "Checking…" with no indication *what* was running or that it hadn't hung; (5) the result's own
    `source_path` line duplicated the just-clicked candidate's path immediately below it; (6) this
    app never refreshes the board list on its own when KiCad's own state changes underneath it (a
    real, standing IPC limitation, not a bug -- the fix is surfacing that constraint to the user
    explicitly via the Refresh button's own copy, not hiding it). Fixed by merging the two "nothing
    to check yet" states into one calm, neutral-styled guidance card (still distinguishing genuine
    launch failures, which get their own dedicated, visible error state so they aren't lost);
    adding a "Switch to KiCad" action plus explanatory copy to the multi-board picker; adding
    `break-all` to the path text; naming the board being checked in the loading copy; and
    highlighting (via `aria-pressed`) the selected candidate instead of repeating its path under the
    result.

*   **Deviation 4: two more real gaps, found by the same user continuing to actually use the app --
    a component-lifecycle bug this context's own design never considered, and a real UX-consistency
    question with a genuine, previously-confirmed technical answer.** (1) Switching to a different
    area tab and back to PCB threw away a check that had just finished. Root cause: `App.tsx`
    conditionally rendered `<BoardAdvisor />` only while `view.area === 'pcb'`, so every tab switch
    away unmounted it and every switch back remounted a fresh instance with no memory of what came
    before. This was never a `BoardAdvisor.tsx` bug -- the component's own state management was
    already correct: this repo's own established per-area conditional-render pattern (`Overview`,
    `EnclosurePanel`, `ComponentDiscovery` are each mounted only while selected) was the wrong
    pattern for a component that runs a genuinely slow, real operation (kicad-cli plus an LLM call)
    someone might reasonably want to glance away from mid-check or right after. Fixed by keeping
    `BoardAdvisor` mounted for the whole lifetime of a project view instead, hidden via CSS
    (`hidden` class) when a different area is selected, and passing it the real `projectName` so it
    can tell an actual project switch (state should reset) apart from a tab switch (state should
    not). (2) The user asked why "Check Schematic" opens a native file dialog instead of offering
    the same live, always-shown list the Board section does. This is not an inconsistency to fix --
    `SPEC-309` §2 already confirmed, live, against a real running KiCad 10.0.3 instance, that
    `get_open_documents(DocumentType.DOCTYPE_SCHEMATIC)` raises the same `"no handler available"`
    `ApiError` regardless of whether a schematic is open, unlike the PCB case where that same error
    only occurs when nothing is open yet -- KiCad's own IPC server has never implemented schematic
    listing at all, at least as of this version. Re-confirming that finding live was not possible
    this round (KiCad was not running on this dev machine at the time), so this fix is explanatory,
    not architectural: added real, factual copy next to "Check Schematic" naming the actual
    constraint and citing where it was confirmed, plus noting the one genuine advantage of the
    file-picker approach (`kicad-cli` runs as a subprocess against the file directly, needing no
    running KiCad connection at all) rather than leaving the difference looking like an oversight.

*   **Deviation 5: the user pushed past the surface-level fix and asked the real question --
    "why is Schematic checking under the PCB tab at all?" -- which turned into both a genuine
    architectural relocation and a new backend capability, not something this context originally
    planned for.** Three things happened in this round, in order:
    1.  **Live re-testing found a real correction to Deviation 4's own reasoning, not just a
        cosmetic gap.** With KiCad genuinely running and a real project open (`NFC_Reader_ESP32`),
        `get_open_documents(DocumentType.DOCTYPE_PCB)` returned a real, resolved document -- and
        `run_action` (a possible way to remotely drive KiCad's own UI, e.g. its built-in
        "Switch to Schematic Editor" button) turned out to be explicitly marked unstable by kipy's
        own docstring ("not intended for use other than by API developers"), so it was ruled out
        as a foundation for real functionality. Neither of these had been checked with KiCad
        actually running during Deviation 4's own investigation.
    2.  **Reading `SPEC-300` directly (not assumed from `BoardAdvisor.tsx`'s own prior comment)
        found the actual root cause of the PCB/Schematic mismatch.** `SPEC-300` §2's own stage
        machine names a `[3] Schematic Advisor` stage before `[4] PCB Advisor` and explicitly lists
        `SPEC-309` as populating it -- ERC checking was always supposed to live under "Schematic,"
        not "PCB." `CTX-305.1` wired the Schematic tab's placeholder to `SPEC-308` instead (drafted
        before `SPEC-309` existed), and `CTX-309.2` found that tab already "claimed" and put both
        checks under "PCB" instead -- a real, traceable drift between two independently-reasonable
        decisions, not a mistake either context made in isolation. Presented this finding to the
        user directly (not silently restructured); they chose moving the Schematic check to its own
        tab, matching `SPEC-300`'s original design.
    3.  **A real, stable alternative to live IPC schematic listing was found and verified, not
        assumed.** `get_open_documents(DocumentType.DOCTYPE_PCB)`'s own real `project.name`/
        `project.path` fields let a schematic path be *derived* (KiCad's own project convention
        always names the root schematic after the project, not the individual board file -- true
        even for a multi-board project whose PCB files are named differently) and then verified to
        actually exist on disk before ever being offered. Confirmed live against the real
        `NFC_Reader_ESP32` project on this machine: the derived path exactly matched the real,
        existing `.kicad_sch` file. This gives Schematic checking nearly the same list-first
        experience as Board checking without needing KiCad's IPC to ever support schematic listing
        at all -- a better outcome than either the original blind file-dialog or the abandoned
        `run_action`-based approach.
    Implemented as: `kicad_bridge.list_project_schematics()` (backend derivation + existence
    check), `daemon.kicad_list_project_schematics()` (route mirroring `kicad_list_open_boards`'s
    shape), a new `SchematicAdvisor.tsx` component (extracted from `BoardAdvisor.tsx`'s old
    Schematic section, rebuilt to the same list-first pattern, with a manual "Pick file manually…"
    fallback retained for a schematic not tied to any open board), a new shared `ViolationsList.tsx`
    (both advisors' own violation rendering, previously duplicated), and `App.tsx` now mounting
    `SchematicAdvisor` in its own Schematic tab instead of `BoardAdvisor`'s old Schematic section.
