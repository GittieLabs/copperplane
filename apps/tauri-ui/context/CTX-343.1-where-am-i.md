---
id: CTX-343.1
spec_ref: "../specs/SPEC-343-the-guided-path.md"
title: "Implementation Context: Where Am I"
status: In-Progress
author: "AI Agent (Claude Opus 5)"
branch: "spec/SPEC-343-the-guided-path"
created: 2026-09-10
last_touched: 2026-09-10
version_included: "v0.7.0"
commit_hashes:
  - "6d1f3db - Plan, with the reading measured before it is built"
---

# CTX-343.1: Where Am I

> **Spec Reference:** Links to
> [SPEC-343](../specs/SPEC-343-the-guided-path.md)

## 1. Feature Definition & Execution Plan

`SPEC-343` §2.4 claims stage is computable from the project record, with nothing new stored and
nothing for the user to declare. Everything else in that spec rests on it: one sentence saying where
someone is, one action that moves them forward, and a non-linear flow that reports its own
regressions.

**So the first question is whether that claim survives real projects**, and it has two halves that
fail differently:

*   **Is the reading unambiguous?** If two states are true at once — a schematic checked and a board
    that has never been opened — the surface has to pick, and picking wrong is worse than saying
    nothing. §3: a guided surface speaks with the app's whole voice, so its first sentence is where
    trust is spent fastest.
*   **Is there a real next action, or does every state collapse to "run a check"?** If it does, this
    is a button that already exists on the tab it points at, and the honest outcome is a smaller
    feature.

Both are measurable today against the maintainer's three real projects and the four this session has
already read. That is the `CTX-341.1` and `CTX-328.1` ordering: measure the assumption the design
rests on before building the thing that rests on it.

### Phase 1: Does the record actually say where you are — DONE

*   [x] Compute a stage reading for every real project on this machine, from the record alone.
*   [x] **Count the ambiguous ones** — states where more than one reading is defensible — rather
        than reporting the ones that worked.
*   [x] For each state, write down the next action it implies, and mark the ones that are just
        *"run the check on the tab this points at"*.
*   [x] Judge against a bar written before the run, in this commit: a reading is useful when it is
        unambiguous **and** its action is something the user would not already be looking at.
*   [x] Write the finding into `SPEC-343` §2, including if it says build less.

### Phase 2: The reading, as a computed fact

*   [ ] A pure function over the project record — no new storage, nothing declared, per §2.4.
*   [ ] **A stage regression is reported, not inferred from ordering.** `SPEC-339`'s `stale_reason`
        already says the schematic moved after the PCB check; §2.4's whole claim is that the DAG
        reports itself.
*   [ ] Every state names the evidence that produced it, so a wrong reading is debuggable by the
        person who sees it rather than only by its author.

### Phase 3: One sentence and one action on Overview

*   [ ] Rendered on Overview, beside the existing cards, never in front of the tabs (§2.1).
*   [ ] The action is a link to a tab that already exists. It never navigates on its own, never
        advances a stage, never mutates the project — `SPEC-300`'s AI boundary applies to this
        surface even though it is not an AI surface, because the user cannot tell the difference.
*   [ ] What it says when nothing is wrong, which §2.6 leaves open and §3 warns about: generic
        praise is worse than silence.

### Phase 4: The toggle, and what off means

*   [ ] Off per project, on at any point, per §5.
*   [ ] Settles §2.6's open question: quiet entirely, or *where am I* without the teaching.
*   [ ] Settles whether a new project defaults it on.

### Phase 5: Absence-shaped triggers — deliberately its own phase

*   [ ] `SPEC-343` §2.6 names this as where the value probably is, and `CTX-210.1` withdrew a pack of
        exactly this shape for being ungroundable. What a novice is missing is usually a part that is
        not there.
*   [ ] The rule to settle: an absence must still name something real (`SPEC-210` §2.2). *"No
        capacitor on `+5V`"* names `+5V`; *"no ESD protection"* names nothing and must not be said.
*   [ ] Measured against a board broken on purpose, with false positives counted, per `SPEC-210` §3.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | A project with no intent reads as the earliest stage, not as an error | `services/python-daemon/tests/test_library_store.py` | ⏳ Pending |
| `TEST-002` | A stale check reports a regression rather than the stage that produced it | `services/python-daemon/tests/test_library_store.py` | ⏳ Pending |
| `TEST-003` | Every reading names the evidence it was computed from | `services/python-daemon/tests/test_library_store.py` | ⏳ Pending |
| `TEST-004` | Two defensible readings never both win silently | `services/python-daemon/tests/test_library_store.py` | ⏳ Pending |
| `TEST-005` | The Overview surface offers a link and never navigates itself | `apps/tauri-ui/src/components/Overview.test.tsx` | ⏳ Pending |
| `TEST-006` | Turning the guided path off leaves Overview as it was | `apps/tauri-ui/src/components/Overview.test.tsx` | ⏳ Pending |

Phase 1 has **no row**, for the reason `CTX-328.1` and `CTX-210.1` both had none: it is a
measurement, and scoring it against an assertion chosen after seeing the answer is not a test.

`TEST-004` is the one that matters. Every other row checks the reading is right; that one checks it
is not confidently right about the wrong thing, which §3 says is how this surface spends trust
fastest.

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-09-10 | Planning | Context drafted; the reading measured before it is built | `6d1f3db` |
| 2026-09-10 | Phase 1 | The reading measured; both predictions wrong | *(this commit)* |

---

## 4. Plan Drift & Architectural Changes

*   **Prediction 1 was wrong about where the ambiguity is.** I predicted it would sit at *schematic
    checked, board never opened*, and expected that to be common. That state is **not ambiguous** —
    it says "check the board" and nothing else is defensible.

    The ambiguity is one state earlier and rarer than predicted: **2 of 64** enumerated combinations,
    both of them *nothing checked at all*, where "check the schematic" and "check the board" are
    equally reasonable. Being wrong here was cheap and it moved the design: the surface needs one
    tie-break rule, not a general disambiguation strategy.

*   **Prediction 2 was right, and its consequence is bigger than the prediction.** I expected most
    next actions to be *"run the check on the tab this points at"*. Four of six are. Only *say what
    you are building* and *link a KiCad project* are actions the user could not already be looking
    at.

    What I did not predict is that **this does not shrink the feature, it inverts it**. The reading
    is not thin even where the action is: *"your schematic changed after the PCB check"* is something
    **no tab can say**, because no tab knows about two stages at once. So the "where am I" line is
    the feature and "what's next" is a convenience attached to it — the reverse of what `SPEC-343`
    §5 implies, and written into that spec as §2.0.

*   **Ordering is a real decision and the enumeration proved it.** Staleness reaches 32 of 64
    combinations because a stale check currently outranks everything. That is wrong at least once: a
    project where the user never said what they are building *and* has a stale check should say the
    first. `SPEC-210` §2.6 left the same ranking question open; they are one question and get one
    answer.

*   **The measurement is an enumeration, not a sample, and that is a real limit.** Three real
    projects exist on this machine and two are in near-identical states. Enumerating proves the
    reading is *decidable*; it says nothing about which states a real user actually spends time in,
    which is what would tell us whether the tie-break above matters at all.

*   **(Original wording of Prediction 1, for the record)** The ambiguity is real and concentrated in
    one place — a project with a linked
    KiCad project, a schematic that has been checked, and a board that has never been opened. Both
    *"check your board"* and *"your schematic has findings to read"* are defensible, and I expect
    that state to be common rather than exotic, because it is what every project looks like halfway
    through.

*   **(Original wording of Prediction 2, for the record)** Most next actions will be "run the check
    on the tab this points at", and that is fine for exactly one state and thin for the rest. If Phase 1 bears that out, the honest shape
    is smaller than §5 implies: a *where am I* line without a *what next* action, which is still
    worth having and is a different feature from the one the spec describes.

*   **This context is on the spec's own branch, not its own.** `/new-context` says to branch from
    `origin/develop` and re-check the spec survived — it would not, since `SPEC-343` is not merged.
    Recorded because it is a deviation from the documented flow rather than an oversight.
