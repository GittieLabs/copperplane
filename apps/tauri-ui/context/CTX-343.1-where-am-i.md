---
id: CTX-343.1
spec_ref: "../specs/SPEC-343-the-guided-path.md"
title: "Implementation Context: Where Am I"
status: In-Progress
author: "AI Agent (Claude Opus 5)"
branch: "spec/SPEC-343-the-guided-path"
created: 2026-09-10
last_touched: 2026-09-10
version_included: "v0.7.0"
commit_hashes:
  - "f8d9220 - Plan, with the reading measured before it is built"
  - "b3daee3 - A skipped description, and a field unknown AND underivable"
  - "49c3a6a - Where a project stands, computed from its record"
---

# CTX-343.1: Where Am I

> **Spec Reference:** Links to
> [SPEC-343](../specs/SPEC-343-the-guided-path.md)

## 1. Feature Definition & Execution Plan

`SPEC-343` §2.4 claims stage is computable from the project record, with nothing new stored and
nothing for the user to declare. Everything else in that spec rests on it: one sentence saying where
someone is, one action that moves them forward, and a non-linear flow that reports its own
regressions.

**So the first question is whether that claim survives real projects**, and it has two halves that
fail differently:

*   **Is the reading unambiguous?** If two states are true at once — a schematic checked and a board
    that has never been opened — the surface has to pick, and picking wrong is worse than saying
    nothing. §3: a guided surface speaks with the app's whole voice, so its first sentence is where
    trust is spent fastest.
*   **Is there a real next action, or does every state collapse to "run a check"?** If it does, this
    is a button that already exists on the tab it points at, and the honest outcome is a smaller
    feature.

Both are measurable today against the maintainer's three real projects and the four this session has
already read. That is the `CTX-341.1` and `CTX-328.1` ordering: measure the assumption the design
rests on before building the thing that rests on it.

### Phase 1: Does the record actually say where you are — DONE

*   [x] Compute a stage reading for every real project on this machine, from the record alone.
*   [x] **Count the ambiguous ones** — states where more than one reading is defensible — rather
        than reporting the ones that worked.
*   [x] For each state, write down the next action it implies, and mark the ones that are just
        *"run the check on the tab this points at"*.
*   [x] Judge against a bar written before the run, in this commit: a reading is useful when it is
        unambiguous **and** its action is something the user would not already be looking at.
*   [x] Write the finding into `SPEC-343` §2, including if it says build less.

### Phase 2: The reading, as a computed fact — DONE

*   [x] A pure function over the project record — no new storage, nothing declared, per §2.4.
*   [x] **A stage regression is reported, not inferred from ordering.** `SPEC-339`'s `stale_reason`
        already says the schematic moved after the PCB check; §2.4's whole claim is that the DAG
        reports itself.
*   [x] Every state names the evidence that produced it, so a wrong reading is debuggable by the
        person who sees it rather than only by its author.

### Phase 3: One sentence and one action on Overview — DONE

*   [x] Rendered on Overview, beside the existing cards, never in front of the tabs (§2.1).
*   [x] The action is a link to a tab that already exists. It never navigates on its own, never
        advances a stage, never mutates the project — `SPEC-300`'s AI boundary applies to this
        surface even though it is not an AI surface, because the user cannot tell the difference.
*   [x] What it says when nothing is wrong, which §2.6 leaves open and §3 warns about: generic
        praise is worse than silence.

### Phase 4: The toggle, and what off means — DONE

*   [x] Off per project, on at any point, per §5.
*   [x] Settles §2.6's open question: quiet entirely, or *where am I* without the teaching.
*   [x] Settles whether a new project defaults it on.

### Phase 5: Absence-shaped triggers — deliberately its own phase, and now with a real first case — DONE

*   [x] `SPEC-343` §2.6 names this as where the value probably is, and `CTX-210.1` withdrew a pack of
        exactly this shape for being ungroundable. What a novice is missing is usually a part that is
        not there.
*   [x] The rule to settle: an absence must still name something real (`SPEC-210` §2.2). *"No
        capacitor on `+5V`"* names `+5V`; *"no ESD protection"* names nothing and must not be said.
*   [x] Measured against a board broken on purpose, with false positives counted, per `SPEC-210` §3.
*   [x] **First case, real rather than hypothetical:** `R1` has `value='R'` on the maintainer's own
        board — KiCad's placeholder, never set. That absence names something real, and it is what
        blocks the current estimate `SPEC-210` §2.5 promises. See `SPEC-343` §2.5.2.

### Phase 6: The description a user skipped

*   [ ] The Overview callout argues the benefit rather than noting the gap, and is **dismissible** —
        a callout that cannot be dismissed becomes furniture, and furniture is not read.
*   [ ] The response caveat, with both disciplines from `SPEC-343` §2.5.1 enforced rather than
        edited: only on a response that would genuinely have changed, and once per conversation
        rather than once per turn.
*   [ ] Never a guess. *"I would have answered better knowing what you are building"* ships; *"this
        looks like an LED blinker"* does not, however confident the app could be.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | A project with no intent reads as the earliest stage, not as an error | `services/python-daemon/tests/test_library_store.py` | ✅ Passed |
| `TEST-002` | A stale check reports a regression rather than the stage that produced it | `services/python-daemon/tests/test_library_store.py` | ✅ Passed |
| `TEST-003` | Every reading names the evidence it was computed from | `services/python-daemon/tests/test_library_store.py` | ✅ Passed |
| `TEST-004` | Two defensible readings never both win silently | `services/python-daemon/tests/test_library_store.py` | ✅ Passed |
| `TEST-005` | The Overview surface offers a link and never navigates itself | `apps/tauri-ui/src/components/Overview.test.tsx` | ✅ Passed |
| `TEST-006` | Turning the guided path off leaves Overview as it was | `apps/tauri-ui/src/components/Overview.test.tsx` | ✅ Passed |

Phase 1 has **no row**, for the reason `CTX-328.1` and `CTX-210.1` both had none: it is a
measurement, and scoring it against an assertion chosen after seeing the answer is not a test.

`TEST-004` is the one that matters. Every other row checks the reading is right; that one checks it
is not confidently right about the wrong thing, which §3 says is how this surface spends trust
fastest.

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-09-10 | Planning | Context drafted; the reading measured before it is built | `f8d9220` |
| 2026-09-10 | Phase 1 | The reading measured; both predictions wrong | `b3daee3` |
| 2026-09-10 | Phase 2 | The reading as a computed fact, with the ranking Phase 1 found wrong | `49c3a6a` |
| 2026-09-10 | Phase 3 | The sentence on Overview, leading, with the action attached | `9064e6a` |
| 2026-09-10 | Phase 4 | The toggle, and both open questions about it settled | *(this commit)* |

---

## 4. Plan Drift & Architectural Changes

*   **Prediction 1 was wrong about where the ambiguity is.** I predicted it would sit at *schematic
    checked, board never opened*, and expected that to be common. That state is **not ambiguous** —
    it says "check the board" and nothing else is defensible.

    The ambiguity is one state earlier and rarer than predicted: **2 of 64** enumerated combinations,
    both of them *nothing checked at all*, where "check the schematic" and "check the board" are
    equally reasonable. Being wrong here was cheap and it moved the design: the surface needs one
    tie-break rule, not a general disambiguation strategy.

*   **Prediction 2 was right, and its consequence is bigger than the prediction.** I expected most
    next actions to be *"run the check on the tab this points at"*. Four of six are. Only *say what
    you are building* and *link a KiCad project* are actions the user could not already be looking
    at.

    What I did not predict is that **this does not shrink the feature, it inverts it**. The reading
    is not thin even where the action is: *"your schematic changed after the PCB check"* is something
    **no tab can say**, because no tab knows about two stages at once. So the "where am I" line is
    the feature and "what's next" is a convenience attached to it — the reverse of what `SPEC-343`
    §5 implies, and written into that spec as §2.0.

*   **Ordering is a real decision and the enumeration proved it.** Staleness reaches 32 of 64
    combinations because a stale check currently outranks everything. That is wrong at least once: a
    project where the user never said what they are building *and* has a stale check should say the
    first. `SPEC-210` §2.6 left the same ranking question open; they are one question and get one
    answer.

*   **The measurement is an enumeration, not a sample, and that is a real limit.** Three real
    projects exist on this machine and two are in near-identical states. Enumerating proves the
    reading is *decidable*; it says nothing about which states a real user actually spends time in,
    which is what would tell us whether the tie-break above matters at all.

*   **(Original wording of Prediction 1, for the record)** The ambiguity is real and concentrated in
    one place — a project with a linked
    KiCad project, a schematic that has been checked, and a board that has never been opened. Both
    *"check your board"* and *"your schematic has findings to read"* are defensible, and I expect
    that state to be common rather than exotic, because it is what every project looks like halfway
    through.

*   **(Original wording of Prediction 2, for the record)** Most next actions will be "run the check
    on the tab this points at", and that is fine for exactly one state and thin for the rest. If Phase 1 bears that out, the honest shape
    is smaller than §5 implies: a *where am I* line without a *what next* action, which is still
    worth having and is a different feature from the one the spec describes.

*   **This context is on the spec's own branch, not its own.** `/new-context` says to branch from
    `origin/develop` and re-check the spec survived — it would not, since `SPEC-343` is not merged.
    Recorded because it is a deviation from the documented flow rather than an oversight.


*   **A finding that changes what "the user does not know" means.** `SPEC-210` §2.5 says an explicit
    "I do not know" on `current_budget` triggers an estimate from the parts on the board. Measured
    against the maintainer's own schematic: **that estimate is not computable there at all.**

    `R1` carries `value='R'` and `D1` carries `value='LED'` — KiCad's placeholders, never set. With no
    resistance there is no current, and deriving one means inventing the value. So *"the user does
    not know"* and *"the app cannot derive it"* are **simultaneously true on the board this entire
    family is written about**, and no spec had that state in it.

    Four fallbacks now sit in `SPEC-343` §2.5.2, and the last two are the interesting ones. An
    absence that names something real is a legitimate consideration, so *"`R1` has no value"* turns
    *"we cannot help"* into *"set this and it becomes answerable"* — which gives Phase 5 a first case
    that is real rather than hypothetical. And when every fallback fails, the honest answer is that a
    maker with a breadboard can measure it: the app distinguishing what it can compute from what only
    the bench can say, the same judgement `SPEC-212` made about a guessed datasheet URL.

*   **The supply bounds the current even when the current is unknown**, which I had not considered
    before looking. `power:+5V` is a real symbol on the schematic, so the rail voltage is a file fact,
    and a supply source caps what a board can draw. That is enough for a worst-case trace-width check
    *stated as worst case* — useful, and available today. Its numbers are a `cited` claim and must be
    read from the USB specification rather than recalled, the same discipline §2.0.1 records for
    IPC-2221 and for the same reason.


*   **The ranking Phase 1 found wrong is fixed, and the list is the ranking.** A first version put
    staleness above everything, reaching 32 of 64 states. `project_stage` now orders by **what the
    app cannot work without**: with no stated goal every downstream answer is generic, so `no_goal`
    outranks a stale check, which is merely out of date. The precedence is a list of named constants
    rather than nested conditions, so changing the answer means moving a line and the order is
    reviewable as a list.

    That answers `SPEC-210` §2.6 and `SPEC-343` §2.6's shared ordering question with a stated
    principle rather than a preference.

*   **`read` takes staleness as an argument and does no file I/O**, so the ranking is testable
    without a filesystem. The one thing that needs the disk — whether a review is behind its source —
    is gathered by the route, best-effort per area: an area whose staleness cannot be determined must
    not cost the user the whole reading.

*   **Every reading names its evidence**, which is not decoration. `SPEC-343` §3 says a guided
    surface that is wrong once is worse than none; a wrong reading has to be debuggable by whoever is
    looking at it rather than only by whoever wrote the ranking.

*   **The route is registered on both modules it needs**, applying `CTX-210.1` Phase 4-5's lesson
    without repeating it. `project.stage` needs `project_stage` and `library_store`; registered on
    one guard it would exist whenever that one imported and fail with a bare `AttributeError` when
    the other did not.

*   **An existing test caught a gap I would not have thought of.**
    `test_ui_route_parity` requires every module the daemon can degrade to have a plain-language
    description in the UI — `SPEC-407` §5, so the notice names what is lost in the user's terms
    rather than naming a Python module. Adding `project_stage` to `_note_degraded` without adding
    its description failed that test immediately. Not a crash, as the test's own docstring says: a
    worse sentence at the worst moment.

*   **Caught at the third attempt, and it is the same test both earlier times almost certainly were.**
    Twice in this session a full-suite run reported an error and I re-ran before capturing it —
    `ci-flake-read-the-failing-attempt` names exactly that, and I walked into it twice.

    Captured on the failing attempt at last: `test_llm_providers.TestRealProviderCalls.
    test_002_real_google_chat`, asserting `usage["output_tokens"] > 0` against a real Google call
    that returned `None`.

    **And then I mischaracterised it in the other direction.** On one failing re-run I called it
    consistent rather than intermittent — a single run is no more a pattern than a single pass is.
    Three subsequent runs passed, and a direct call returned `{'input_tokens': 8, 'output_tokens':
    1}`. It is intermittent: Google sometimes omits the field.

    **It is invisible to CI**, which is why every check has been green all day: `test_llm_providers`
    loads `.env.local` for these real-provider tests and skips without a key, and CI has none. So
    this fails only on a maintainer's machine, which is the worst place for a test to be unreliable
    and the easiest place for it to be dismissed.

    Unrelated to this branch — nothing here touches `llm_providers` — and tracked separately. Left
    unfixed on purpose: the assertion's own docstring says it exists because *"a provider's SDK
    version drifting could silently zero this out"*, so loosening it defeats its point, and the real
    fix is a decision about that test rather than a patch made in passing.


*   **Phase 1's finding shaped the layout, not just the copy.** The sentence leads and the action
    sits beside it as a link, because the reading is what no tab can produce. A first sketch had the
    action as the heading with the sentence beneath as justification, which reads as a to-do item —
    and a to-do item implies a checklist with a bottom, which `SPEC-343` §3 names as a risk because a
    board is never finished.

*   **The surface renders nothing when the reading fails, and says nothing about why.** A guided
    surface that is wrong once is worse than none (§3); one that puts a daemon error on the Overview
    tab is worse still, and the error is not the user's to act on. Pinned by a test, because "renders
    nothing" is the behaviour most likely to be broken later by someone adding a helpful fallback.

*   **`complete` congratulates nobody**, and the test asserts the absence rather than the presence —
    it fails if the words *well done*, *great*, *nice work* or *looks good* ever appear. §2.6 leaves
    open what this state should say and §3 warns generic praise is worse than silence, so an
    assertion that catches the tempting wrong answer is worth more than one that pins the current
    right one.

*   **`onGoToArea` is owned by `App`**, exactly as `onCarryToSearch` is: Overview names a
    destination, App moves. That keeps `SPEC-300`'s boundary — nothing "decides which screen the user
    is on" — enforced by structure rather than by discipline, since Overview has no way to navigate
    even if a later change wanted it to.

*   **The reading re-reads when the record changes**, keyed on `intent` and `kicad_project_path`. A
    stale reading is the exact bug this surface exists to prevent having, and it would have been an
    easy one to ship: the first version keyed on `projectName` alone and would have kept saying *"you
    haven't said what you're building"* immediately after the user said it.


*   **Settled: off means gone, not diminished.** `SPEC-343` §2.6 asked whether off should still show
    *where am I* without the teaching. It should not. A user who switches something off and gets a
    smaller version of it is being negotiated with, and one toggle with one meaning is easier to
    trust than a three-state control nobody asked for.

    The consequence is a real design problem the question did not mention: **if off leaves no trace,
    the setting is unfindable by exactly the user who most needs to undo it.** So off leaves one
    quiet line where the card was -- *"Show where this project stands"* -- a way back without being
    the feature it replaced.

*   **Settled: it defaults on, and the default is not a coin toss.** The people this surface exists
    for are the ones who will not go looking for a setting to switch it on. Defaulting off costs them
    the whole feature; defaulting on costs a second-time user one click. It is one sentence, so being
    wrong in this direction is cheap and being wrong in the other is not.

*   **Off does not spend a route call.** A reading nobody will see should not be computed, and the
    test asserts `projectStage` was never called rather than only that nothing rendered -- the
    difference is invisible on screen and would have been easy to leave in.

*   **The record backfills `guided_path: True` explicitly** rather than every caller reading
    `.get(key, True)`. Unlike `intent_fields`, where "never asked" is a meaningful third state, a
    toggle has two positions, and a stored default says what is true rather than leaving a convention
    to be remembered.

*   **The shape assertion caught the new key for the fourth time today**, after `intent_fields`,
    `considerations` and this. An assertion that enumerates a record is worth more than one that
    spot-checks it, and the evidence is that it keeps working.

*   **Settled: an absence is sayable when it is an absence OF something, ON something the files
    name.** *"No capacitor on `+5V`"* names `+5V`. *"`R1` has no value"* names `R1`. *"No ESD
    protection"* names nothing and is not said, however true it might be. `SPEC-210` §2.2 needed no
    extension for this — the absence changes what is claimed, not what must be named.

*   **The obvious version was wrong by a factor of eleven, and measuring it first is the only reason
    that is a sentence rather than a shipped feature.** KiCad writes a symbol's own name into its
    `value` property by default, so *"value equals symbol name"* looks like a perfect never-set
    detector. On the maintainer's **correct** board it matches **all eleven symbols**:

    | symbol | why the naive rule is wrong about it |
    | :--- | :--- |
    | `power:GND`, `power:+5V` | the value **is** the net name, and is correct |
    | four `Mechanical:MountingHole` | a hole has no value to set |
    | `MCU_Module:Arduino_UNO_R3` | the value **is** the part number |
    | `Switch:SW_Push`, `Device:LED` | nothing downstream computes from these |

    Narrowed to a **closed list** of symbols whose value is an electrical parameter something
    calculates with — not a prefix match, because `Device:R_Potentiometer` is a resistor whose value
    means something different, and guessing from the name is exactly how the eleven happened. On the
    same board: **one**, and it is `R1`, which genuinely has no value.

*   **This is the third time in this session that a plausible rule failed on a real board**, after
    the LED pack firing on `GND` and the decoupling pack failing to tell a rail from ground. Three
    is a pattern rather than three incidents: **a rule written from plausibility fails on first
    contact, and the measurement that catches it costs one command.** That belongs in `SPEC-210` §3's
    argument, which currently says a pack must clear the bar without saying how reliably the bar is
    hit.

*   **My own test pinned the wrong thing and failed for a good reason.**
    `CTX-210.1` Phase 5 asserted `len(PACKS["power"]) == 1` to record that a pack had been withdrawn.
    The decision was *that pack stays out*, not *there is exactly one pack forever* — so adding a
    second legitimate one failed an assertion that was not protecting anything. Rewritten to assert
    the withdrawn pack is absent by name, which is the decision it meant to pin.

*   **What was not verified.** The known live-provider flake appeared again and is named this time:
    `test_llm_providers.TestRealProviderCalls.test_002_real_google_chat`, unrelated to this branch
    and invisible to CI. Everything else passes at 1,409.
