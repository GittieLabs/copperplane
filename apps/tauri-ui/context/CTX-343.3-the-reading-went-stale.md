---
id: CTX-343.3
spec_ref: "../specs/SPEC-343-the-guided-path.md"
title: "Implementation Context: The Reading Went Stale"
status: Completed
author: "AI Agent (Claude Opus 5)"
branch: "fix/SPEC-343-overview-goes-stale"
created: 2026-09-12
last_touched: 2026-09-12
version_included: "v0.7.0"
commit_hashes:
  - "08dd41a - The Overview reading went stale when another tab changed it"
---

# CTX-343.3: The Reading Went Stale

> **Spec Reference:** Links to
> [SPEC-343](../specs/SPEC-343-the-guided-path.md)

A defect, not a feature — and the one `SPEC-343` §6 predicted when it closed:

> **What is not [verified]:** nobody has used this surface as a novice would.

Reported the day after, from ordinary use. Overview said *"Nothing has been checked yet"* and offered
**Check the schematic**. The maintainer followed it, ran the review on the Schematic tab, came back,
and the card had not moved.

---

## 1. Feature Definition & Execution Plan

### Phase 1: Reproduce it as a test before touching the component — DONE

*   [x] Prove the record and the daemon were already right, so the search stays above them.
*   [x] A test that fails on the real sequence: visible, hidden, visible again with the same props.
*   [x] A second test for the other half — a hidden tab must not re-read.

### Phase 2: Re-read on becoming visible — DONE

*   [x] `active` through `Overview`, `WhereYouAre` and `WhatYouGotRight`.
*   [x] `App` supplies it from the area it already tracks.
*   [x] Leave the visible card alone while hidden rather than blanking it, so returning does not
      flash an empty box.

### Phase 3: The evidence line stopped restating its own heading — DONE

*   [x] Evidence says what the record **held**, never what the reading concluded.
*   [x] The rule written into `read()`'s docstring, so the next state added does not repeat it.
*   [x] A test pinning the half that is checkable.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-016` | The reading re-reads when the tab becomes visible again | `apps/tauri-ui/src/components/Overview.test.tsx` | ✅ Passed |
| `TEST-017` | The considerations re-read on return too, for the same reason | `apps/tauri-ui/src/components/Overview.test.tsx` | ✅ Passed |
| `TEST-018` | Neither re-reads while the tab stays hidden | `apps/tauri-ui/src/components/Overview.test.tsx` | ✅ Passed |
| `TEST-020` | Every evidence line names something the record held | `services/python-daemon/tests/test_project_stage.py` | ✅ Passed |
| `TEST-021` | The check would notice, run against the exact string it replaced | `services/python-daemon/tests/test_project_stage.py` | ✅ Passed |
| `TEST-022` | The finished reading says why it is not a stale one | `services/python-daemon/tests/test_project_stage.py` | ✅ Passed |
| `TEST-023` | No two states share an evidence line | `services/python-daemon/tests/test_project_stage.py` | ✅ Passed |

`TEST-018` is the one to read the Plan Drift about. Its first version could not fail.

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-09-12 | Phases 1-2 | The reading re-reads when the tab becomes visible | `08dd41a` |
| 2026-09-12 | Phase 3 | Evidence says what the record held, not what the reading concluded | *(this commit)* |

---

## 4. Deviations & Discoveries (Plan Drift)

*   **The daemon was right the whole time, and starting there saved the investigation.** The record
      on disk held `last_reviews: ['schematic']`, and `project_stage.read` computed *"the schematic
      has been checked and the board has not"* from it. Confirming that first meant the search never
      went near the Python, the packs, or the persistence — all of which were working.

*   **Three independent causes, and no single one of them is the bug.** This is why "add
    `last_reviews` to the dependency array" would have been the wrong fix even though it sounds like
    the right one:

    1.  `App.tsx` renders every area at once and hides the inactive ones with **CSS**. Overview never
        unmounts, so nothing re-runs on its own.
    2.  Nothing refetches the project record on navigation.
    3.  The effect's dependencies are the fields Overview changes **itself** — `intent`,
        `kicad_project_path`, `guided_path`. A check run elsewhere moves `last_results` and
        `last_reviews`, which nothing on that page ever sees.

    Fixing only (3) leaves the prop stale from (2). Fixing only (2) still needs (3) to re-run.
    Re-reading on becoming visible sidesteps all three, needs no sibling tab to announce anything,
    and covers a record changed outside the app entirely.

*   **One of my tests could not fail, and the probe is worth recording.** `TEST-018` asserted the
    guard by checking `not.toHaveBeenCalled()` straight after `render`. It passed **before the fix
    existed**. Probing a plain mount showed why: the effect is async, and `projectStage` has been
    called **zero** times at that instant regardless of any guard. It now waits for something that
    does resolve first, so a broken guard has had every chance to fire before the assertion denies it
    did. `CLAUDE.md`'s *"a check that cannot fail is not evidence"*, caught by deliberately looking
    for it rather than by luck.

*   **971 tests passed over a type error.** `active` went into the destructure of `WhereYouAre`
    without being added to its type. Vitest does not typecheck, so the suite was green and `tsc -b`
    was not. Both are needed; neither substitutes.

*   **Verified as a user, which is the whole point of this context existing.** The maintainer walked
    the full progression in the real app: `nothing checked` → **Check the schematic** → `board_only`
    → **Check the board** → `both_checked` → **Generate an enclosure** → `complete`. Four live
    transitions.

    Worth being exact about what the automated evidence was worth here. Every route verified, every
    pack measured against real boards, 968 frontend tests green — and the surface was broken in the
    first minute of somebody using it, in a way none of that could have caught.

*   **The fix made a second defect visible, which is the ordinary way of these things.** With the
    reading finally reaching `complete`, the card read:

        Schematic, board and enclosure have all been checked.
        schematic, board and enclosure have all been checked

    The heading and the line beneath it, word for word. Three other states restated themselves the
    same way in weaker form. Nobody had seen it because nobody had reached those states — the
    staleness bug stopped the reading moving at all.

*   **Nothing could have caught it, and that is worth being precise about.** The heading is written
    by `sentenceFor` in `Overview.tsx`; the evidence by `project_stage.py`. Two languages, two test
    suites, neither able to see the other. The duplication only exists on screen, which is where it
    was found — from a screenshot.

    So the test pins the half that IS checkable: evidence must name something the **record held**
    (`recorded`, `on record`, `linked`, `changed`), which a restatement of the conclusion never does.
    `TEST-021` runs that check against the exact string it replaced, so the guard is known to notice
    rather than assumed to.

*   **`complete` gained the fact a reader actually wants there.** It now says *"results are on record
    for all three, and none of them is out of date"* — answering why this is not `regressed`, which
    is the only real question at a state that outranks nothing.
