---
id: CTX-340.1
spec_ref: "../specs/SPEC-340-ordering-this-board-from-a-real-house.md"
title: "Implementation Context: Choosing a Board House, and Seeing What It Changes"
status: Review
author: "AI Agent (Claude Opus 5)"
branch: "feat/CTX-340.1-choosing-a-board-house"
created: 2026-09-08
last_touched: 2026-09-08
version_included: "v0.6.0"
commit_hashes:
  - "1967ab7 - Context drafted, Phase 1 persistence"
  - "7ae1f2d - Phases 2-5: client, profile picker, before-and-after, PCB wiring"
  - "8d6978d - Phase 5: fixed the dead end the click-through found"
  - "045b5f7 - Phase 5: folded the check into the board check"
---

# CTX-340.1: Implementation Context: Choosing a Board House, and Seeing What It Changes

> **Spec Reference:** Links to
> [SPEC-340](../specs/SPEC-340-ordering-this-board-from-a-real-house.md)

## 1. Feature Definition & Execution Plan

`SPEC-114` is built, merged, and verified against the frozen sidecar. Not one line of it is
reachable by a person. This context is the whole distance between those two facts, and it is
mostly wiring — which is the point. `CTX-114.1` deliberately put the dangerous, measured work
behind three routes so that this part could be small.

The order below front-loads the two things that are *not* wiring: persistence (because a profile
belongs to a project, and getting that wrong means a user silently checks board B against board A's
house) and the result surface's three states (because `SPEC-340` section 2.1 names that as the only
real design decision in the spec).

### Phase 1: The profile belongs to the project

*   [x] Persist the chosen profile on the project record in `library_store`, alongside `intent` and
    `last_results`, round-tripping through `load_project`/`save_project` so `CTX-312.1`'s
    pointer/manifest routing comes free rather than being re-derived.
*   [x] A `project.set_fabrication_profile` route that validates before it stores. Storing an
    unvalidated profile would push the failure to review time, where it reads as a broken check
    rather than a bad number.
*   [x] A profile travels with a linked project folder, and does not leak between projects. Test
    both directions explicitly — this is the failure that would silently check the wrong board
    against the wrong house.

### Phase 2: The client

*   [x] `lib/fabricationReview.ts` wrapping the three routes, with the result types mirrored from
    the daemon rather than re-guessed. `boardAdvisor.ts` already types `ignored_checks` as
    `{ key, description }`, which is the shape the daemon actually returns — reuse it, do not
    redeclare it differently.
*   [x] `fabrication.review_board` goes through `submitJob` from `lib/ipc.ts`, like `checkBoard`
    does. It is in `ASYNC_ROUTES` and takes roughly five seconds.
*   [x] Surface `SidecarDiscarded` as its own error, distinct from a transport failure. The user
    needs to know their rules did not run, not that something went wrong.

### Phase 3: Choosing the house

*   [x] A picker offering the unbranded starting point or the user's own numbers, per project.
*   [x] Each field shows the date it was recorded and whether it has been confirmed. The bundled
    profile ships with all nine fields unconfirmed precisely so this has something true to say.
*   [x] Name the process the numbers describe, next to the numbers — `SPEC-340` section 3's
    mitigation for the too-strict-profile risk.
*   [x] Say plainly which recorded numbers can never be checked, and why. The daemon already returns
    the sentences via `describe_unenforceable`; render them rather than inventing new wording.
*   [x] An empty state for a project with no linked board, which is honest rather than an error
    (`SPEC-114` section 2.9).

### Phase 4: The before and after

*   [x] Both counts, always together. Never the after-count alone: `SPEC-340` section 2 is explicit
    that "27 findings" on its own reads as a broken board.
*   [x] Group findings by what the fab would do — built silently wrong first, then rejected, then
    cosmetic. The daemon ranks them already; preserve that order rather than re-sorting.
*   [x] The three verification states, which is section 2.1's named decision. `applied` shows the
    result. `indeterminate` shows the result **and** says the rules could not be observed taking
    effect, without reading as either a pass or a failure. A discarded sidecar shows no result at
    all, because there is nothing trustworthy to show.
*   [x] Render `not_checked` in full: ignored checks, profile rules gated off by them, and
    unenforceable fields. Reuse `ViolationsList`'s existing ignored-checks treatment from
    `SPEC-332`.
*   [x] No string anywhere in this phase tells the user the board is good to order.

### Phase 5: Into the PCB area, and in front of a person

*   [x] Mount in `BoardAdvisor`, which already owns board selection and stays mounted across tab
    switches.
*   [x] Reset profile state on a genuine project switch, not a tab switch — the distinction
    `BoardAdvisor` already draws for its check result, and for the same reason.
*   [x] **A real human click-through in the running native window**, against the tutorial board,
    with the outcome recorded here. Not the automated suite. `SPEC-340` section 5 states the bar:
    someone watches the count go from 4 to 27 and can say which findings would have come back as a
    working board that was quietly wrong. Until that happens this context is not done, however
    green the tests are.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | A profile round trips on the project record and survives reload | `services/python-daemon/tests/test_capability_profile.py` | ✅ Passed |
| `TEST-002` | A profile set on one project is not visible from another | `services/python-daemon/tests/test_capability_profile.py` | ✅ Passed |
| `TEST-003` | Setting an invalid profile is refused at store time, not at review time | `services/python-daemon/tests/test_capability_profile.py` | ✅ Passed |
| `TEST-004` | A linked project carries its profile in the folder manifest | `services/python-daemon/tests/test_capability_profile.py` | ✅ Passed |
| `TEST-005` | The client dispatches review through the async job path | `apps/tauri-ui/src/lib/fabricationReview.test.ts` | ✅ Passed |
| `TEST-006` | A discarded sidecar surfaces as its own error, not a transport failure | `apps/tauri-ui/src/lib/fabricationReview.test.ts` | ✅ Passed |
| `TEST-007` | The client reuses the daemon's ignored-checks shape rather than a second one | `apps/tauri-ui/src/lib/fabricationReview.test.ts` | ✅ Passed |
| `TEST-008` | Each field shows its recorded date and confirmation state | `apps/tauri-ui/src/components/FabricationProfile.test.tsx` | ✅ Passed |
| `TEST-009` | The bundled starting point is labelled as attributed to no vendor | `apps/tauri-ui/src/components/FabricationProfile.test.tsx` | ✅ Passed |
| `TEST-010` | Numbers that can never be checked are named, with the reason | `apps/tauri-ui/src/components/FabricationProfile.test.tsx` | ✅ Passed |
| `TEST-011` | A project with no linked board gets an empty state, not an error | `apps/tauri-ui/src/components/FabricationProfile.test.tsx` | ✅ Passed |
| `TEST-012` | Both counts render together, never the after-count alone | `apps/tauri-ui/src/components/FabricationSummaryBanner.test.tsx` | ✅ Passed |
| `TEST-019` | The profile reaches the one board check instead of a second one | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | ✅ Passed |
| `TEST-020` | Each section names the rules it uses before it is run | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | ✅ Passed |
| `TEST-013` | Findings keep the daemon's ranking, silently-wrong first | `apps/tauri-ui/src/components/FabricationSummaryBanner.test.tsx` | ✅ Passed |
| `TEST-014` | An indeterminate result reads as neither a pass nor a failure | `apps/tauri-ui/src/components/FabricationSummaryBanner.test.tsx` | ✅ Passed |
| `TEST-015` | A discarded sidecar surfaces as a check error, not a result | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | ✅ Passed |
| `TEST-016` | Ignored checks and gated-off rules are both rendered | `apps/tauri-ui/src/components/FabricationSummaryBanner.test.tsx` | ✅ Passed |
| `TEST-017` | No rendered string tells the user the board is good to order | `apps/tauri-ui/src/components/FabricationSummaryBanner.test.tsx` | ✅ Passed |
| `TEST-018` | Profile state resets on a project switch but not a tab switch | `apps/tauri-ui/src/components/BoardAdvisor.test.tsx` | ✅ Passed |

`TEST-017` is worth stating as a test rather than a review habit. `SPEC-340` section 3 names this
screen as the one place that sentence wants to be written, and an assertion over the rendered output
is the only thing that keeps it out as the component grows.

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-09-08 | Planning | Context drafted; persistence and the third state front-loaded | `1967ab7` |
| 2026-09-08 | Phase 1 | Profile persists per project, validated at store time | `1967ab7` |
| 2026-09-08 | Phases 2-5 | Client, profile picker, before-and-after, PCB wiring | `7ae1f2d` |
| 2026-09-08 | Phase 5 | Click-through: fixed the dead end it found | `8d6978d` |
| 2026-09-08 | Phase 5 | Second click-through: folded the check into the board check | `045b5f7` |

---

## 4. Plan Drift & Architectural Changes

*Document any deviations from the original SPEC definitions encountered during build time.*

*   **Deviation 1: `dispatch_to_daemon_typed` does not exist.** The client was first written
    against an invented Tauri command. The real convention is `dispatch(method, params)` from
    `lib/ipc.ts` with an explicit `response.error` check, exactly as `listOpenBoards` does, and
    `submitJob` only for `ASYNC_ROUTES` members. Caught by reading `boardAdvisor.ts` rather than by
    a test, because a test written against the same wrong assumption would have mocked it happily.
    The same pass caught `handle.completion`, which is really `handle.result`.

*   **Deviation 2: `@testing-library/user-event` is not a dependency of this app.** Every existing
    component test drives clicks with `fireEvent` from `@testing-library/react`. Switched to match
    rather than adding a dependency to make one new test file's style work.

*   **Deviation 3: the "never says good to order" guard was too blunt to ship as written.** It
    initially forbade the bare word `passed`, which matched the ignored-checks note -- "your board
    can look clean because a check is off rather than because it passed" -- a sentence that says the
    *opposite* of a verdict. It then caught `ready to order` inside the disclaimer "that is not the
    same as a board being ready to order". Rather than weaken the guard with negative lookbehind,
    the component copy was reworded to "not a verdict on the whole board", so the assertion can stay
    a plain substring check. A guard that has to understand negation is a guard that will eventually
    be wrong.

*   **Deviation 4: adding a project field broke an exhaustive-shape test, correctly.**
    `test_library_store`'s `test_003_an_unlinked_project_still_behaves_exactly_as_before_ctx_312_1`
    asserts the entire loaded record, and its comment names every field added since `CTX-312.1`.
    `fabrication_profile` was added to both, following that pattern -- this test is doing its job,
    and the right response is to extend the list deliberately, not to loosen the assertion.

*   **Observed, not caused by this work:** `test_llm_providers`'
    `test_002_real_google_chat` failed once during a full-suite run with
    `TypeError: '>' not supported between instances of 'NoneType' and 'int'` -- Google's live API
    returned `output_tokens: None`. It passed on an isolated re-run, and this branch touches no
    provider code. Flagged as its own task rather than papered over here.

*   **Deviation 5, and the reason Phase 5 exists: the click-through found a dead end the whole
    test suite was blind to.** With a house chosen and no board yet picked, the PCB area rendered
    the nine configured numbers and then *nothing* -- no Check button, no explanation, straight on
    to the AI review panel. The block was gated on `profile && selectedBoard`, so the entire
    affordance vanished silently rather than saying what to do next. Someone had just configured a
    profile and was left with no way forward and no sentence explaining why.

    Every component test passed throughout. They exercised "no profile" and "profile and board",
    and never the state in between, which is the one a real person hits first because choosing the
    house is the natural first move on that screen. A `profile && !selectedBoard` branch now says
    "Pick a board above to check it against <house>", with two regression tests: one for the missing
    guidance and one asserting the Check button appears once both are chosen. The first was
    confirmed to fail against the pre-fix component before being kept -- a check that cannot fail is
    not evidence.

*   **Prediction 2 confirmed.** It predicted the click-through would find at least one thing the
    component tests could not, on the grounds that `CTX-109.3`, `CTX-301.2`, `CTX-305.2`,
    `CTX-306.x` and `CTX-311.16` all did. It did, on the first run, and the defect was a dead end
    rather than a cosmetic flaw. Worth noting the shape: not a wrong value or a broken call, but a
    state that rendered nothing and therefore could not be noticed by any assertion about what *was*
    rendered.

*   **Deviation 6, from the same click-through, and the larger of the two: the feature was built as
    a third overlapping tool.** With a profile chosen, the PCB tab offered *three* things that each
    ran DRC: the Board (DRC) section (KiCad's rules, findings explained one by one), this feature's
    own "Check against <house>" button (KiCad's rules **and** the fab's, before-and-after, no
    explanations), and `SPEC-319`'s "Review the board" (which runs DRC again inside the review
    agent). Nothing on screen said what any of them checked, how they differed, or whether a user
    needed all three -- and the maintainer's reaction was exactly that: *"the check seems to show
    very similar info to what the review board does. what is the disconnect?"*

    The diagnosis is that a capability profile is not a second question about the board. It is a
    change to which rules the *existing* question uses. Built as its own surface, it necessarily
    duplicated the board check and then lost the per-finding explanations on the way, which is why
    it read as both redundant and worse.

    `kicad.check_board` now takes an optional `profile`. Absent, it behaves exactly as before.
    Present, it checks against the house and returns a `fabrication` block that `ViolationsList`
    renders as a banner above the same explained findings list. The separate route, button, result
    component and client function are all deleted. `SPEC-340` §2 was rewritten to say this, because
    it is a spec-level correction and not implementation drift.

    This is `SPEC-302`'s failure mode reproduced by a spec written specifically to avoid it, which
    is worth recording plainly: having `## 5. User & Interaction` filled in did not prevent it. The
    section described what the user does on *this* surface and never asked what else was already on
    the screen doing something similar.

*   **Deviation 7: a linked project knew its board and still asked the user to pick one.**
    `linkedProjectBoard` resolves exactly one board and the app holds its path, but `selectedBoard`
    was only ever set inside the click handler, so everything downstream behaved as though no board
    existed. Reported directly: *"we linked a proj and a board is there. i should not have to pick
    a board for this to work."* The linked board is now selected when it resolves, which does not
    run the check -- it just stops the app pretending not to know which board this is.

*   **Deviation 8: every section now says what it checks and what comes back.** Requested in the
    same message. The Board (DRC) section names the rules it will use *before* it runs -- KiCad's
    defaults, or the chosen house by name -- and the profile card says plainly that it does not run
    a check of its own. Cheap, and the absence of it is most of what made the tab confusing.

    Also removed one duplication this exposed: the banner does not list KiCad's switched-off tests,
    because `SPEC-332`'s existing block directly below already does. Its own list is the profile's
    ungated limits, which is a different thing.

*   **Prediction 1 was half right.** Persistence was indeed where the risk sat, but not for the
    reason predicted: project identity resolution was fine, and the real cost was the exhaustive
    shape test above. The `indeterminate` state, predicted as the harder half, was
    straightforward once the copy was written -- the difficulty was in the wording, not the code.


*   **Prediction 1, recorded before implementation starts:** most of this will be genuinely
    uneventful, because `CTX-114.1` already absorbed the measurement risk. The two places that
    prediction is most likely to be wrong are per-project persistence, where `CTX-110.1` and
    `CTX-304.2` both found real bugs in how project identity resolves, and the `indeterminate`
    state, which has no precedent in this UI to copy.
*   **Prediction 2:** the click-through in Phase 5 will find at least one thing the component tests
    cannot. That has happened in `CTX-109.3`, `CTX-301.2`, `CTX-305.2`, `CTX-306.x` and
    `CTX-311.16`, and there is no reason this surface is the exception. If it finds nothing, that
    is itself worth recording, because it would be the first time.

---

## 5. What was verified, and what was not

**Verified.** The full daemon suite (1252 tests) and the full frontend suite (885 tests, 51 files)
are green, and `tsc --noEmit` is clean. The four routes this surface depends on were driven into the
**rebuilt frozen sidecar** over JSON-RPC: `fabrication.generic_profile` returns its unbranded
starting point, and `project.set_fabrication_profile` fails with a storage-configuration error
rather than `-32601`, which is what proves it is registered. Both probes carried a negative control
-- a deliberately misspelled route returning `-32601` -- so a pass means the probe could have
detected a missing route.

**Used by a person, in the real native window.** The maintainer ran the app and reached the PCB
area with a profile configured. That run found Deviation 5 above -- a dead end no test had covered
-- which is exactly what this phase is for. The profile panel itself rendered correctly: the
unbranded house name, all nine values, each marked "not confirmed" with its recorded date, and the
line saying nine of them did not come from the user's own board house.

**Not verified.**

*   **The full path end to end.** What has been seen working in the real app is choosing a house and
    the state immediately after it. Picking a board, running the check, and reading the 4-to-27
    result in the native window has not been observed. `SPEC-340` section 5's actual bar -- someone
    naming which findings would have come back as a working board that was quietly wrong -- is
    therefore still unmet, even though the dead end blocking the path is now fixed.
*   **A browser preview cannot substitute.** The Tauri `invoke` bridge is absent there, so every
    `fabrication.*` call would fail and the surface would render its empty state. Reporting that as
    verification would be precisely the kind of check that cannot fail.

**Also not verified:** everything `CTX-114.1` listed -- one machine, one board, KiCad 10.0.3, macOS
aarch64 -- still holds, unchanged, underneath this.
