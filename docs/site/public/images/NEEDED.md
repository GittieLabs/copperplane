# Screenshots still needed

Captured from a real project with real data (`test 1`, an NFC reader board) and
redacted (personal username/paths blacked out) before saving. Done:
`settings.png`, `component-search.png`, `design-guidance.png`, `enclosure.png`,
`library.png`, `board-check.png` — the last two are saved here but not yet
referenced from any page.

Still needed:

| File | What to show |
| :--- | :--- |
| `hero.png` | The app with a project open, wide, showing the rail and an area tab |

**Before capturing:** check no API key, no personal file paths and no client
project names are visible.

Save as PNG, ideally under 400 KB each. Reference them from a page as
`/copperplane/images/<name>.png`.
