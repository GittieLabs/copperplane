---
title: Copperplane
description: A co-pilot for your first real circuit board. It reads your KiCad project, explains what the checks are telling you, and sizes a case that fits.
template: splash
hero:
  tagline: Your breadboard works. Copperplane helps with the next part — a real board, checks you can actually read, and a case that fits it. It works alongside KiCad and FreeCAD, not instead of them.
  actions:
    - text: Why this exists
      link: /copperplane/why/
      icon: right-arrow
    - text: Install
      link: /copperplane/install/
      variant: minimal
    - text: View on GitHub
      link: https://github.com/GittieLabs/copperplane
      icon: external
      variant: minimal
---

import { Card, CardGrid, Aside } from '@astrojs/starlight/components'

<Aside type="caution" title="Early, and honest about it">
Every release publishes a build for **macOS (both architectures), Windows and
Linux**. The macOS builds are signed and notarized; the Windows and Linux ones
are not signed yet, so each release also ships a `SHA256SUMS.txt` you can
[check your download against](/copperplane/install/#verify-your-download).

Every live test against real KiCad and FreeCAD has run on exactly one machine, a
Mac. So if you are on **Windows or Linux**, you can find things nobody here can
find, and [telling us what happened](https://github.com/GittieLabs/copperplane/issues/new?template=platform_report.yml)
is the single most useful thing you can do for this project right now — more
useful than a pull request.
</Aside>

![Copperplane reading a real KiCad project](/copperplane/images/hero.png)

## What it does

<CardGrid>
  <Card title="Reads the datasheet, and shows you where">
    Ask what a part needs around it — supply range, decoupling, reset, crystal
    load capacitance, layout constraints — and get a plain-language answer with
    the datasheet page it came from, one click away. If it cannot cite it, it
    does not say it.
  </Card>
  <Card title="Finds footprints you already have">
    Searches every KiCad footprint library installed on your machine, including
    the 150-odd that ship with KiCad itself, plus your own saved library and
    curated community libraries — merged into one ranked result set.
  </Card>
  <Card title="Explains what the checker is telling you">
    Runs ERC and DRC through KiCad's own `kicad-cli` and translates the result
    into plain language with suggested fixes. It reads your board; it does not
    edit it.
  </Card>
  <Card title="Builds an enclosure from the real board">
    Takes your board's actual outline and mounting-hole positions and generates
    a printable enclosure with standoffs and an optional lid, previewed in 3D
    and exportable to STEP.
  </Card>
</CardGrid>

## What it is not

It is an **advisor with hands, not a replacement CAD tool**. It does not draw
your schematic, place your components, or route your board, and none of that is
planned. [The full boundary is written down](/copperplane/what-it-is/)
— worth two minutes before you install anything.

## Where your data goes

Your parts library is readable JSON on your own disk. Your API keys are in the
operating system keychain, never in a config file. The only thing that leaves
your machine is the prompt you send to the model provider you chose — and you
can choose a local one. [The specifics](/copperplane/privacy/).

## Built on

KiCad and FreeCAD do the actual engineering. This app talks to KiCad over its
own IPC API and drives FreeCAD headlessly — it bundles neither and replaces
neither. [How that works, and what we owe them](/copperplane/built-on/).
