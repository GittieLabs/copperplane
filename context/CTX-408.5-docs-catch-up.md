---
id: CTX-408.5
spec_ref: "specs/SPEC-408-messaging-for-the-maker-who-is-leveling-up.md"
title: "Implementation Context: The Docs Describe An App That Changed"
status: In-Progress
author: "AI Agent (Claude Opus 5)"
branch: "feat/CTX-408.5-docs-catch-up"
created: 2026-09-11
last_touched: 2026-09-11
version_included: "v0.7.0"
commit_hashes:
  - "58af84f - The guard, and the five claims it caught"
---

# CTX-408.5: The Docs Describe An App That Changed

> **Spec Reference:** Links to
> [SPEC-408](../specs/SPEC-408-messaging-for-the-maker-who-is-leveling-up.md)

Reported by the maintainer: *"the documentation is stale… parts describing kicad must be open or
that the app can determine if a schematic is open are still in the docs."*

The audit found more than stale phrasing. **The docs are organised around KiCad's live IPC as the
main path, and it has become the minor one.** ERC, DRC, schematic reading, the netlist, board
geometry, the enclosure and all eight consideration packs now work on closed files. IPC is left
holding four things: writing a footprint into an open board, listing open boards, resolving 3D model
paths, and the live-board enclosure route that a file-based one already duplicates.

Two claims are not merely dated but **false**, and both sit in the place a sceptical reader goes to
find out what the app cannot do.

---

## 1. Feature Definition & Execution Plan

### Phase 1: A test that fails when the docs contradict the daemon — DONE

*   [x] Tie specific "not built" claims to the routes that falsify them.
*   [x] Fail loudly, naming the page and the route, so the next drift is caught by CI rather than by
      a user reading it.
*   [x] Written **before** the corrections, so it is seen failing on the real stale text.

### Phase 2: Correct every stale capability claim — DONE

*   [x] `what-it-is.md` no longer lists reading a schematic under what is not built.
*   [x] `board-checks.md` no longer says there is no review that volunteers concerns.
*   [x] `board-checks.md` no longer says a schematic must be picked every time.
*   [x] `install.md`, `first-run.md` and `built-on.md` describe the IPC server as what it now is:
      needed for a few live-session features, not for the app to read your work.
*   [x] `enclosure.md` and `footprints.md` stop presenting the live board as the primary route.

### Phase 3: The tutorial, rebuilt around what the app teaches — DONE

*   [x] Stop touring tabs. The current shape walks Overview, PCB, Schematic, Enclosure in order,
      which is the app's navigation rather than the user's question.
*   [x] Lead with what the app noticed about the reader's own board and why each finding matters.
*   [x] Keep every command and path verifiable against the real example project.

### Phase 4: A shot list someone can actually hold a camera to — DONE

*   [x] `SCREENSHOTS.md` describes reality: which existing shots are dead and why, which surfaces
      are new since 2026-09-04 and have never been captured.
*   [x] Per-shot setup state, since several new surfaces only appear when a project is in a
      particular condition.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | A page claiming a capability is missing fails when the daemon registers the route providing it | `services/python-daemon/tests/test_docs_claims_match_daemon.py` | ✅ Passed |
| `TEST-002` | The check would notice, exercised against the real stale sentences this context removes | `services/python-daemon/tests/test_docs_claims_match_daemon.py` | ✅ Passed |
| `TEST-005` | Every route named by the guard is one the daemon actually has | `services/python-daemon/tests/test_docs_claims_match_daemon.py` | ✅ Passed |
| `TEST-006` | A rewrapped sentence is still caught, so a reflow cannot disarm a row | `services/python-daemon/tests/test_docs_claims_match_daemon.py` | ✅ Passed |
| `TEST-003` | Every referenced image still exists and every image is still referenced | `services/python-daemon/tests/test_docs_images.py` | ✅ Passed |
| `TEST-004` | Every relative link in the authored docs resolves | `services/python-daemon/tests/test_readme_claims.py` | ✅ Passed |

`TEST-001` is the point of the phase order. The existing suite checks names, versions, links and
images — everything **about** a page except whether what it says is true. That is exactly the gap
`what-it-is.md` fell through: it has been listing *"Reading your schematic's contents"* under
**"What is not built yet"** while the netlist reader it denies has been shipping features all week.

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-09-11 | Phases 1-2 | The guard, and the five claims it caught | `58af84f` |
| 2026-09-11 | Phases 3-4 | The tutorial led by what the app noticed, and an honest shot list | *(this commit)* |

---

## 4. Deviations & Discoveries (Plan Drift)

*   **The guard caught my own mistake on its first run, before it ever saw a docs page.** I wrote
    `schematic.list_components` in the table; the real route is `kicad.list_schematic_components`.
    `TEST-005` — the row asserting every named route exists — failed immediately.

    Without it that row would have been permanently disarmed: a phrase paired with a route that
    never matches can never fire, and the test would have passed forever while guarding nothing.
    That is `CLAUDE.md`'s *"a check that cannot fail is not evidence"*, and it cost one line to
    prevent.

*   **Written before the corrections, and it failed on all five real claims**, naming page and
    route:

        what-it-is.md says "reading your schematic's contents", but the daemon registers
          'kicad.list_schematic_components'
        guides/board-checks.md says 'no ai review of your schematic', but the daemon registers
          'project.considerations'

    Seeing it fail on the genuine stale text is the only way to know the phrases match what is
    actually written rather than what I remembered writing.

*   **The rot was structural, not a set of dated sentences.** The docs are organised around KiCad's
    live IPC as *the* way the app works — `built-on.md`'s "How it talks to KiCad" opened with
    *"Over KiCad's own IPC API… The app connects to a running KiCad instance"* — and that has
    quietly become the minor path. ERC, DRC, connectivity, board geometry, the enclosure and all
    eight consideration packs read files with KiCad closed. IPC is left with four things.

    So `install.md` and `built-on.md` needed reordering, not patching: files first, IPC second and
    explicitly optional, with the four live-session features listed by name.

*   **The biggest new capability had no page at all.** Eight consideration packs, the claim classes,
    the trigger discipline and the guided-path toggle were documented nowhere. `board-checks.md`
    said the opposite — *"no AI review of your schematic yet… real planned work"* — which is the
    single most misleading sentence on the site, because it sits where a sceptical reader goes to
    find out what the app cannot do.

    Added `guides/what-it-notices.md`, and a homepage card, since the four existing cards described
    an app without its most distinctive feature.

*   **Two pages I expected to be wrong were already right.** `guides/enclosure.md` leads with the
    file-based route and mentions the live board second; `guides/footprints.md` correctly frames
    injection as the one genuinely live feature. Worth recording, because the audit's value depends
    on it having actually checked rather than assumed a uniform rot.

*   **The tutorial was better than the report suggested, and checking saved me from "fixing" it
    wrongly.** Its opening calls the board's LED an RGB LED, which looked like an error — the symbol
    is a plain `Device:LED`. It is not an error. The *footprint* is `LED_THT:LED_D5.0mm-4_RGB`, and
    that mismatch is the tutorial's entire central lesson. Correcting it would have destroyed the
    point of the piece.

    Its error counts hold up too, verified by running the real checks against the shipped example:
    DRC gives four annular-width violations plus one unconnected item — the "five errors on the PCB"
    it claims — and ERC gives two.

*   **What was actually wrong was the shape.** The tutorial walked the tabs in order and reached the
    app's own front door, Overview, only as an item in a list of five. Everything the app now says
    unprompted was missing.

    Rebuilt so the reader links the project and is immediately told three things: R1's placeholder
    value, the `+5V` rail sitting on the Arduino's `VIN` pin, and — deliberately — the one thing the
    board got right. All three are real output from the tutorial's own example project, run through
    the real packs rather than written from memory.

    The existing `D1`/`SW1` narrative is kept intact. It was the best writing on the site and it
    needed reframing, not replacing: *"the two findings KiCad never makes"* became *"two more"*,
    because there are now five such things on that board.

*   **The screenshots have a thirteen-minute story in them.** `welcome.png` and `no-project.png`
    were captured at 23:26 on 2026-09-04. `CTX-338.1` — the commit that put the Copperplane mark on
    those two exact screens — landed at 23:39. The shots are of the screens the commit was about,
    taken a quarter of an hour before it.

    Eleven of the eighteen are dead, each tied in the shot list to the commit that invalidated it so
    the claim can be re-derived rather than believed. Four are probably fine and say so, with
    "check, do not assume" rather than a blessing.

*   **The real gap is not the dead shots but the missing ones.** Seven surfaces have never been
    photographed because they did not exist on 2026-09-04, and they include every part of the
    guided path. The site cannot show the app's most distinctive feature at all. Those are listed
    with the project state needed to produce each, since several only appear when a project is in a
    particular condition.

*   **`SCREENSHOTS.md` was itself stale in the way it warned about.** It opened with *"Every
    existing image has to be retaken… all six images"* — written for a set replaced a week ago, and
    then it said "Fifteen images" in a document listing eighteen. A file whose job is to track what
    is out of date had gone out of date, which is an argument for `TEST-001` covering prose that
    makes checkable claims, not only published pages.
