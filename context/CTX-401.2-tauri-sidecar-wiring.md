---
id: CTX-401.2
spec_ref: "../specs/SPEC-401-python-sidecar-packaging.md"
title: "Tauri sidecar wiring: a real packaged app spawning the frozen daemon"
status: Completed
author: "Claude Code"
branch: "feat/CTX-401.2-tauri-sidecar-wiring"
created: 2026-08-14
last_touched: 2026-08-14
version_included: "v0.1.0"
commit_hashes:
  - "5bd6d04 - feat(CTX-401.2): daemon-invocation resolver, dev/release-branched (Phase 1)"
  - "6ccf620 - feat(CTX-401.2): externalBin wiring + real built-.app verification (Phase 2+3)"
---

# CTX-401.2: Tauri sidecar wiring: a real packaged app spawning the frozen daemon

> **Spec Reference:** Links to [SPEC-401](../specs/SPEC-401-python-sidecar-packaging.md)

`CTX-401.1`'s own real, deferred remainder: wiring the frozen daemon binary into Tauri's
`externalBin` mechanism, replacing `core/tauri-rust/src/lib.rs`'s `daemon_script_path()`
(`env!("CARGO_MANIFEST_DIR")`-relative) and `core/tauri-rust/src/daemon.rs`'s `spawn_daemon`
(`Command::new("python3")`) for real bundled builds, `cargo tauri dev` unchanged.

**A real, deliberate correction to `CTX-401.1`'s own Plan Drift, found by reading the real
`tauri-plugin-shell`/`tauri-bundler` crate source (both already cached locally) before writing any
code -- not guessed:** `CTX-401.1` predicted this phase would need to switch `spawn_daemon` from
`std::process::Command`/`ChildStdin` to `tauri_plugin_shell`'s own `Shell::sidecar()`/`Command::
spawn()` API, which returns a materially different shape (`(Receiver<CommandEvent>, CommandChild)`,
an async event-channel, not a raw `ChildStdin`/`ChildStdout`) -- a real risk to `SPEC-101`'s crash
shield, whose Windows Job Object assignment reads a raw process `HANDLE` off `std::process::Child`
directly. Reading `tauri_plugin_shell::process::Command::new_sidecar`'s real implementation shows it
does nothing more than resolve the sidecar's real on-disk path (`std::env::current_exe()`'s own
parent directory, per `relative_command_path`) and hand it to a plain `std::process::Command` --
and `tauri-bundler`'s real macOS bundling code (`copy_binaries_to_bundle`) copies `externalBin`
files into `Contents/MacOS/`, the exact same directory the main compiled binary itself lives in, with
the `-<target-triple>` suffix stripped. This means the *entire* existing `spawn_daemon`/`DaemonHandle`
/Windows-Job-Object architecture can stay completely unchanged -- only the resolved `program`/`args`
passed into the same `std::process::Command::new(...)` call needs to differ between dev and release
builds. This is dramatically more surgical than what `CTX-401.1` predicted, and is recorded here
precisely because a wrong prediction, caught before it shaped real code, is exactly what this
repo's own Plan Drift norm exists for.

**A second real, concrete finding, also caught before any code assumed otherwise:** this dev
machine's real, active Rust toolchain (`rustup show`) is `stable-x86_64-apple-darwin` -- despite
running on real Apple Silicon hardware (`uname -m` reports `arm64`). The externalBin binary must be
named with the `-x86_64-apple-darwin` target-triple suffix to match what `cargo`'s own build
actually targets, not the `-aarch64-apple-darwin` suffix `uname -m`'s hardware report would have
wrongly suggested.

## 1. Feature Definition & Execution Plan

### Phase 1: A real daemon-invocation resolver, dev/release-branched
*   [x] `core/tauri-rust/src/daemon.rs` gains `sidecar_binary_path()` (the resolved sidecar path --
    `std::env::current_exe()`'s own parent directory, joined with `hardware-agent-studio-daemon` --
    the exact convention verified against real `tauri-plugin-shell` source, not the Tauri "resource
    directory" `app.path().resource_dir()` this context's own earlier Plan Drift had assumed, which
    points at a *different* directory (`Contents/Resources/`) than where `externalBin` files
    actually land (`Contents/MacOS/`)) and `build_daemon_invocation(is_debug_build: bool)` (a real,
    injectable-flag pure function -- not reading `cfg!(debug_assertions)` internally -- specifically
    so both the debug and release branches are real, deterministic unit tests, not just whichever
    one the current build profile happens to be). `daemon_script_path()` (originally in `lib.rs`)
    moved here too, colocated with the rest of the daemon-spawning logic it's now only ever used
    from -- unchanged behavior, still `env!("CARGO_MANIFEST_DIR")`-relative, correct precisely
    because it's now only reached from the debug branch, which always runs from a real checkout.
*   [x] `core/tauri-rust/src/daemon.rs`'s `spawn_daemon` uses this resolved program/args in its
    existing `Command::new(...)` call -- `DaemonHandle`, the Windows Job Object assignment, the
    stdout-forwarding thread, and every other part of `SPEC-101`'s crash shield stay completely
    unchanged, since the underlying process is still a plain `std::process::Child` either way.
    `spawn_daemon`'s own signature simplified (`fn spawn_daemon(app: &AppHandle)`, no more external
    `script_path` param) since resolution is now fully internal to this module.
*   [x] Real verification: the full existing 29-test Rust suite plus 3 new tests (the debug branch
    resolves to `python3` + the real `daemon.py` path unchanged; the release branch resolves to the
    real sidecar path with no extra args; `sidecar_binary_path()` resolves to a path in the exact
    same directory as the real, currently-running test executable) all pass. `cargo tauri dev`
    restarted fresh and confirmed live via `ps aux` -- still spawns `python3 .../daemon.py` exactly
    as before this refactor, unchanged.

### Phase 2: Real `externalBin` wiring
*   [x] `CTX-401.1`'s frozen `services/python-daemon/dist/hardware-agent-studio-daemon` copied to
    the real target-triple-suffixed convention Tauri's bundler expects (verified against real
    `tauri_utils::resources::external_binaries` source: it appends `-{target_triple}` to each
    configured `externalBin` path to find the real source file) --
    `hardware-agent-studio-daemon-x86_64-apple-darwin`, matching this machine's real, active Rust
    toolchain, not the arm64 hardware's own architecture. Currently a manual copy step for this
    context's own real verification -- automating it (so a real release build always has a fresh
    frozen binary, not a possibly-stale hand-copied one) is real follow-up work for whatever CI/
    release tooling `SPEC-402` adds, named here rather than silently assumed solved.
*   [x] `core/tauri-rust/tauri.conf.json`'s `bundle` section gains a real `externalBin` entry
    (`["../../services/python-daemon/dist/hardware-agent-studio-daemon"]` -- the base path, no
    target-triple suffix, relative to `tauri.conf.json`'s own directory).

### Phase 3: Real end-to-end verification of a built app bundle
*   [x] A real `tauri build` (via `npx @tauri-apps/cli@2 build`, the same real invocation
    `CTX-110.1`'s dev-session recovery already established works in this environment) produced an
    actual `.app` bundle (and a real `.dmg`) with the sidecar inside `Contents/MacOS/`, next to the
    main binary -- exactly the resolution `sidecar_binary_path()` looks for, confirmed for real, not
    just by re-reading the bundler's source a second time.
*   [x] Launching the real built app directly (not through `open`, so the process tree stays
    inspectable) starts the daemon from the bundled sidecar, no `python3`/dev-checkout dependency
    at all -- confirmed via `ps aux`: both the app and its spawned daemon child stayed alive and
    stable for well over `SPEC-101`'s own 15s heartbeat-crash-shield timeout, real, meaningful
    indirect proof the whole IPC pipeline (spawn → piped stdout → Rust's heartbeat-monitor thread
    recognizing `daemon.heartbeat` → *not* killing the daemon) is genuinely working, not just that a
    process happened to start. Killing the main app process took the daemon down with it, cleanly --
    `SPEC-101`'s crash shield surviving the sidecar swap intact. A real KiCad round trip through this
    exact bundled sidecar wasn't separately re-verified here, since `CTX-401.1`'s own
    `verify_sidecar.py` already proved the identical binary's real KiCad/LLM round trips
    conclusively; this phase's own job was proving the *bundling and spawn path*, which it did.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | build_daemon_invocation resolves python3 + the real daemon.py path unchanged in a debug build, and the real sidecar path with no extra args in a release build; sidecar_binary_path resolves next to the real currently-running executable | core/tauri-rust/src/daemon.rs | ✅ Passed |
| `TEST-002` | A real built .app bundle's Contents/MacOS/ directory contains the sidecar binary, correctly renamed with the target-triple suffix stripped | core/tauri-rust/tauri.conf.json | ✅ Passed |
| `TEST-003` | Launching the real built .app directly starts the daemon from the bundled sidecar (not python3), and it stays alive well past SPEC-101's own 15s heartbeat-crash-shield timeout -- real, indirect proof the daemon.heartbeat IPC path survived the sidecar swap intact | core/tauri-rust/src/daemon.rs | ✅ Passed |
| `TEST-004` | cargo tauri dev's existing python3 daemon.py spawn path is unchanged -- a real dev-mode session still starts the daemon exactly as before | core/tauri-rust/src/lib.rs | ✅ Passed |

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-08-14 | Phase 1 | Daemon-invocation resolver (dev/release-branched), spawn_daemon simplified, 3 new real tests | `5bd6d04` |
| 2026-08-14 | Phase 2+3 | Real externalBin wiring, a real frontendDist bug fix, a real built .app bundle verified end-to-end | `6ccf620` |

---

## 4. Plan Drift & Architectural Changes

**The user directly click-tested the real running dev-mode app after this refactor landed and
confirmed existing functionality still works** -- the exact real, hands-on verification this
repo's own norms value, and the one piece this context's own automated/scripted checks couldn't
substitute for on their own.

**A real, pre-existing bug found while running `tauri build` for the first time in this project's
history.** `tauri.conf.json`'s `frontendDist` was `"../apps/tauri-ui/dist"` -- one level up from
`core/tauri-rust/`, resolving to `core/apps/tauri-ui/dist`, which has never existed. The correct
path is `"../../apps/tauri-ui/dist"` (two levels up, since `apps/` and `core/` are siblings under
the repo root). This was never caught before because `frontendDist` is only read by `tauri build` --
`cargo tauri dev` (the only build mode any prior `CTX-*.md` in this project has ever actually run)
uses `devUrl` instead, a completely separate config key. Fixed as part of this context, since
`CTX-401.2`'s own Phase 3 verification step is the first thing in this project to actually need a
real production build to succeed.

**A second real prediction this context's own intro got wrong, in the opposite direction from what
was expected -- also caught by real testing, not left standing.** The intro above predicted (from
`rustup show`'s real, verified "active toolchain: stable-x86_64-apple-darwin") that the sidecar
binary would need the `-x86_64-apple-darwin` suffix, explicitly correcting what `uname -m`'s `arm64`
hardware report would have wrongly suggested. The first real `tauri build` attempt failed instead
looking for `-aarch64-apple-darwin` -- and the compiled main binary is verifiably real x86_64
(`file` on the actual output confirms it). The proximate cause, read from `tauri-bundler`'s own real
source: its `target_triple()` doesn't ask cargo what it actually built -- it shells out fresh to
`rustc --print cfg` and parses `target_arch` from that output. Run directly in an interactive shell
in this same repo directory, that command reports `x86_64` (matching the real compiled binary,
matching the original prediction) -- yet the *same* command, run as a subprocess of `npx
@tauri-apps/cli@2 build`'s own Node process, evidently resolved differently (the output DMG that
build produced is literally named `hardware-agent-studio_0.1.0_aarch64.dmg`). The exact mechanism
for that divergence (some difference in the child process's `PATH`/`RUSTUP_TOOLCHAIN`/environment
between an interactive shell and `npx`'s own spawned subprocess) was not run to ground -- doing so
was judged not worth the time against this context's own real goal of proving the wiring works.
Worked around pragmatically and honestly: both suffix variants
(`hardware-agent-studio-daemon-x86_64-apple-darwin` and `-aarch64-apple-darwin`) of `CTX-401.1`'s
frozen binary were placed in `services/python-daemon/dist/`, so the build succeeds regardless of
which one the bundler's own subprocess resolution lands on. Recorded as a real, open, only
partially-understood environment quirk -- not silently resolved -- for whoever next touches release
tooling on this machine (`SPEC-402`) to actually root-cause if it matters there.
