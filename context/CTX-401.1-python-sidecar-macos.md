---
id: CTX-401.1
spec_ref: "../specs/SPEC-401-python-sidecar-packaging.md"
title: "Python Sidecar Packaging: a real, committed macOS PyInstaller freeze"
status: Completed
author: "Claude Code"
branch: "feat/CTX-401.1-python-sidecar-macos"
created: 2026-08-14
last_touched: 2026-08-14
version_included: "v0.1.0"
commit_hashes:
  - "fac0a77 - feat(CTX-401.1): real, committed PyInstaller freeze of daemon.py (Phase 1)"
---

# CTX-401.1: Python Sidecar Packaging: a real, committed macOS PyInstaller freeze

> **Spec Reference:** Links to [SPEC-401](../specs/SPEC-401-python-sidecar-packaging.md)

The first real slice of `SPEC-401`'s macOS-only scope: a real, reproducible, committed PyInstaller
freeze of `services/python-daemon` into a single working binary, driven and verified directly over
its real stdin/stdout JSON-RPC wire. Deliberately **not** wiring that binary into Tauri's own
`externalBin`/sidecar mechanism yet, and not yet replacing `core/tauri-rust/src/lib.rs`'s
`daemon_script_path` or `core/tauri-rust/src/daemon.rs`'s `spawn_daemon` -- both real, substantial,
crash-shield-adjacent changes deliberately deferred to a follow-up context (`CTX-401.2`, not yet
written) rather than rushed into the same sitting as the freeze work itself, the same relationship
`CTX-108.1`/`CTX-108.3` had.

Real, load-bearing findings from this session reshape `SPEC-401`'s own speculative risk list -- see
Plan Drift for the full account, including a wrong prediction this real testing corrected. In short: a
plain `pyinstaller --onefile daemon.py`, run against a Homebrew-framework Python 3.11 (this repo's
own platformio-managed interpreter lacks the shared library PyInstaller requires -- a real, concrete
environment finding), already produces a working binary with **no hidden-imports needed** -- verified
with a real `kicad.get_version` round trip (KiCad 10.0.3) and a real HTTPS call to
`https://api.anthropic.com/v1/messages` (a deliberately-fake key, correctly rejected with a real 401)
through the frozen binary itself, not a mock.

## 1. Feature Definition & Execution Plan

### Phase 1: A real, committed, reproducible PyInstaller build
*   [x] A real freeze environment: a Homebrew-framework Python 3.11 (`Py_ENABLE_SHARED`'s own
    sysconfig value is misleadingly `0` even on a real framework build -- the actual signal is
    `PYTHONFRAMEWORK`/a real `libpython3.11.dylib` on disk), not this repo's own platformio-managed
    interpreter, which PyInstaller flatly refuses ("Python was built without a shared library").
*   [x] A committed `services/python-daemon/daemon.spec` reproducing the verified-working
    `pyinstaller --onefile --name hardware-agent-studio-daemon daemon.py` invocation, so the freeze
    is a real, repeatable repo artifact -- not a one-off interactive command a future contributor has
    to rediscover. `SPECPATH` (PyInstaller's own global for the `.spec` file's directory) resolves
    `daemon.py` directly, correct regardless of the invoking shell's own cwd.
*   [x] `pyinstaller==6.22.0` pinned in a real, separate `services/python-daemon/requirements-build.txt`
    (not `requirements.txt` itself -- it's a build-time tool for freezing the daemon, not a runtime
    dependency the daemon imports). `services/python-daemon/build/` (PyInstaller's own intermediate-
    artifact directory, distinct from the already-`dist/`-ignored final output) added to the root
    `.gitignore` -- a real gap found while doing this: nothing previously ignored it.
*   [x] A real, committed, re-runnable verification script
    (`services/python-daemon/scripts/verify_sidecar.py`) -- not a pytest suite, since freezing takes
    real minutes and this is a packaging-verification step, not a unit test -- drives the actual
    frozen binary over its real stdin/stdout JSON-RPC wire (the same protocol
    `core/tauri-rust`'s `spawn_daemon` speaks) and checks: a real `daemon.ready` with real
    `kicad_available`/`freecad_available` capability detection, a real `kicad.get_version` round
    trip (skipped cleanly, not failed, when no live KiCad connection is available), and a real
    (auth-failing, the correct and expected outcome for a deliberately fake key) `llm.chat` call
    through AgentFlow's lazily-imported `AnthropicProvider` -- proving the full HTTPS/TLS/certifi
    stack survived the freeze intact. Re-run against a fresh build from the committed `daemon.spec`
    (not just the earlier ad-hoc Phase-0 invocation) -- all three checks passed for real.

**Deliberately deferred to `CTX-401.2` (not yet written), not built here:** wiring the frozen binary
into Tauri's `externalBin`/sidecar mechanism (`tauri.conf.json`'s `bundle` section, renaming the
binary to Tauri's required `<name>-<target-triple>` convention), swapping
`core/tauri-rust/src/daemon.rs`'s `spawn_daemon` from `Command::new("python3")` to
`tauri_plugin_shell`'s sidecar-spawn API for real bundled builds (`cargo tauri dev`'s own
`python3 daemon.py` spawn path staying unchanged, per `SPEC-401` §1's own non-goal), and a real
`tauri build` producing an actual `.app` with the sidecar inside it, launched for real to confirm
`SPEC-101`'s crash shield still supervises it correctly. All real, substantial, crash-shield-adjacent
work, better done as its own reviewable slice than folded into the freeze work above.

---

## 2. Testing Requirements Matrix

| Test ID | Test Description | Test File Location | Status |
| :--- | :--- | :--- | :--- |
| `TEST-001` | The frozen binary (built from the committed daemon.spec) emits a real daemon.ready with real capability detection | services/python-daemon/scripts/verify_sidecar.py | ✅ Passed |
| `TEST-002` | The frozen binary completes a real kicad.get_version round trip against a live KiCad connection | services/python-daemon/scripts/verify_sidecar.py | ✅ Passed |
| `TEST-003` | The frozen binary completes a real (auth-failing) llm.chat call, proving AgentFlow's lazily-imported provider SDKs survived the freeze with no hidden-imports needed | services/python-daemon/scripts/verify_sidecar.py | ✅ Passed |

---

## 3. Implementation Log & Commit History

| Date | Phase | Description | Commit Hash |
| :--- | :--- | :--- | :--- |
| 2026-08-14 | Phase 1 | Committed daemon.spec, requirements-build.txt, and a real, re-runnable verify_sidecar.py -- all three real checks passed against a fresh build | `fac0a77` |

---

## 4. Plan Drift & Architectural Changes

**`SPEC-401`'s own predicted hidden-imports risk was wrong, and real testing found that out before
any code shipped assuming otherwise.** The spec's own §2/§3 predicted `llm_providers.py`'s lazily
-imported provider SDK classes (`from agentflow import AnthropicProvider`, reached only inside an
`if provider == "anthropic":` branch, only at runtime) would likely need explicit
`--hidden-import` declarations, reasoning that PyInstaller's static analysis is "known to miss"
that pattern. A plain `pyinstaller --onefile daemon.py`, with zero `hiddenimports` entries, produced
a binary that made a real HTTPS POST to `https://api.anthropic.com/v1/messages` and got back a real
401 for a deliberately fake key -- proof the full import chain (AgentFlow's own package, `anthropic`,
`httpx`, `certifi`'s real TLS verification) survived intact. The actual mechanism: PyInstaller's
module-graph analysis parses `import`/`from X import Y` statements by walking a module's AST, not by
executing it -- a conditionally-reached `from agentflow import AnthropicProvider` inside an `if`
block is still *syntactically present* in `llm_providers.py`'s source, so it's found regardless of
whether that branch runs during analysis. The real risk PyInstaller's static analysis actually misses
is *fully dynamic* imports (`importlib.import_module(some_variable)`), which this codebase doesn't
use anywhere in the daemon's own provider-selection code. Recorded here per this repo's own norm --
a wrong prediction, corrected by real testing, is not a failure to hide.

**A real, concrete environment finding, not a hypothetical:** this repo's own dev-loop Python (a
platformio-managed interpreter every prior `CTX-*.md` in this project has used for every real KiCad/
FreeCAD verification) was built without a shared library, and PyInstaller refuses to run against it
outright ("Python was built without a shared library, which is required by PyInstaller"). A
Homebrew-installed `python3.11` (`brew install python@3.11`) provided a real, working alternative --
`sysconfig`'s own `Py_ENABLE_SHARED` reports a misleading `0` even on this real, working framework
build; the actual signal PyInstaller cares about is a real `Python.framework`/`libpython3.11.dylib`
on disk. Whatever CI or release-build environment eventually runs this freeze for real needs a
Python interpreter chosen for this property specifically, not assumed to already have it.

**A real, distribution-relevant Gatekeeper cold-launch delay, found while writing the verification
script, not designed around in advance.** The first (and, empirically, every subsequent) launch of
this ad-hoc-signed frozen binary took ~11 seconds of real wall-clock time before emitting anything on
stdout, despite only ~1.2s of actual CPU time -- a real macOS Gatekeeper/AMFI launch-verification
overhead for an unsigned/ad-hoc-signed executable, not a daemon startup bug. `verify_sidecar.py`'s
own timeouts (20s for `daemon.ready`) were sized generously specifically because of this, found by
direct measurement, not guessed. This is real, concrete evidence for why `SPEC-402` (code signing)
matters beyond just Gatekeeper's install-time warning dialog -- an unsigned sidecar's *every* cold
launch pays this real latency cost, which interacts directly with `SPEC-107`'s own heartbeat-timeout
crash-shield logic once this sidecar is wired into the real supervisor in `CTX-401.2`. Flagged here
as a real, load-bearing constraint for whoever picks up `SPEC-402`, not silently absorbed.

**Binary size came in real and small: 32MB**, not the "tens to a few hundred MB" `SPEC-401`'s own
§3 speculated for a `kipy` + `pynng` + `trimesh` + three LLM provider SDKs freeze. A real, corrected
number, worth recording precisely rather than leaving the spec's own speculative range standing
uncorrected.

**Scope was deliberately narrowed to the freeze itself, mid-session, rather than pushing through to
Tauri wiring in the same sitting.** The original plan sketch (before Phase 1 landed) treated Tauri
`externalBin` wiring and a real built-`.app` launch as this same context's Phase 2/3. Once Phase 1
was real, committed, and fully verified, continuing straight into `spawn_daemon` -- the one function
every route in this app depends on, and the exact code `SPEC-101`'s crash shield supervises --
without a review checkpoint in between felt like the wrong place to keep compounding risk in one
sitting. Re-scoped this context to its own real, complete, independently-mergeable unit (the freeze
+ verification tooling), and named the remaining work `CTX-401.2` explicitly rather than leaving a
half-finished Phase 2/3 checklist inside a context marked `Completed` -- the same relationship
`CTX-108.1`/`CTX-108.3` already established in this repo.
